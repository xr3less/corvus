# Build outputs and generated files are never formatted.
# (Mirrors the eslint ignores in eslint.config.mjs — same backstop rule per D-029:
# reviewers may build, but build output must never be left scannable by a gate.)
.next/
dist/
build/
out/
coverage/
*.tsbuildinfo
next-env.d.ts

# Vendored skill sandbox (third-party reference bundle, not product code) —
# mirrors the eslint ignores in eslint.config.mjs. Kept byte-faithful to its
# source, so it is never reformatted.
.github/skills/impeccable/
# Vendored agent definitions shipped with the same bundle; reference-only.
.github/agents/
