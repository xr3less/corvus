# Task Report: persona-a-intent-start (REVIEW)

## VERDICT: PASS

Reviewed by an independent reviewer with clean context. No source file was modified by this
review; the tree is byte-identical to how it was found (proven in Artifacts Confirmed §5).

---

## Artifacts Confirmed

| Artifact | Claimed | Observed on disk | Evidence |
|---|---|---|---|
| `packages/ai/src/persona-prompt.ts` | MODIFIED | MODIFIED, 6973 bytes, mtime 2026-09-25 16:00:23 | `git status --porcelain` → ` M packages/ai/src/persona-prompt.ts`; `git diff --stat` → 31 lines changed |
| `packages/ai/src/persona-prompt.test.ts` | MODIFIED | MODIFIED, mtime 16:01:26 | `git status` → ` M`; diff → 130 lines changed |
| `packages/ai/dist/persona-prompt.js` | REGENERATED by build | REGENERATED, mtime 16:08:36 (my independent `tsc` run) | `git check-ignore` → ignored by `.gitignore:2 dist/`; served resolution confirmed: `require.resolve('@corvus/ai')` → `packages/ai/dist/index.js` |

**1. The served artifact really is dist, not src.** `@corvus/ai`'s `exports["."].default` is
`./dist/index.js`, and a live import of the bare specifier resolved to
`C:\...\corvus\packages\ai\dist\index.js`. Every functional probe below ran against the served
artifact, not the source.

**2. dist was NOT stale.** I hashed dist, re-ran `npm run build --workspace @corvus/ai`, and
re-hashed: `persona-prompt.js` = `c8e07c15bffdc962e42facff7f683c3b39200c6d502e7d8145f6984f09967dc4`
before and after (also `.d.ts` `1abe33a3…`, `index.js` `44f8597e…`, both unchanged). A rebuild is a
no-op, so the pre-existing dist already carried the new prompt. The task's rebuild claim holds.

**3. Fifth file refreshed by the build, matching the report.** `dist/persona-prompt.d.ts`,
`.d.ts.map`, `persona-prompt.test.js` all carry mtime 16:08:36. Public surface in the rebuilt
`.d.ts` is byte-identical in shape to the report's quoted block (`OwnerLanguage`,
`buildPersonaPrompt`, `buildVerdictPrompt`, `buildBriefPrompt`).

**4. No manifest touched, no install run.** `git status --porcelain` for `package.json`,
`package-lock.json`, `packages/ai/package.json`, `apps/web/package.json` is **empty**.
`package-lock.json` mtime is 10:54, hours before this task. Zero new imports were added, so no
dependency was needed. *Honest caveat:* the report's specific evidence "`packages/ai/node_modules`
mtime unchanged at 2026-09-15" I could not re-verify — that directory now reads mtime 16:17
**because my own review created and then removed a scratch dir inside it**. That contamination is
mine, it is disclosed here rather than silently accepted, and manifest cleanliness independently
confirms no install occurred.

**5. Proof this review did not modify the tree.** Post-review: the tracked file list is exactly the
4 in-flight source files + the 2 under review; `dist/persona-prompt.js` still hashes `c8e07c15…`;
my scratch directory no longer exists. No `stash` / `checkout --` / `restore` / `reset` was run at
any point (LESSONS §6.1) — the one temporary file I needed was written outside the repo, and my
guard-break copies were made under a gitignored scratch path that I then deleted.

---

## Functional Checks

Toolchain detected, not assumed: **npm workspaces** (`package-lock.json` present; no
`pnpm-lock.yaml`, `yarn.lock`, or `bun.lockb`). Root scripts are `typecheck` / `lint` / `format` /
`test` / `ci`. `@corvus/ai` scripts are `build` (`tsc`), `test` (`vitest run`), `typecheck`
(`tsc --noEmit`), `format` (prettier). Every check below was run by me, independently.

| # | Check | Command | Observed | Exit |
|---|---|---|---|---|
| 1 | Package typecheck | `npm run typecheck --workspace @corvus/ai` | `tsc --noEmit`, zero errors | 0 |
| 2 | Lint, touched files | `npx eslint packages/ai/src/persona-prompt.ts packages/ai/src/persona-prompt.test.ts --max-warnings 0` | no output | 0 |
| 3 | Package test suite | `npm run test --workspace @corvus/ai` | **161 passed** (6 files); `persona-prompt.test.ts` **45** | 0 |
| 4 | Package build | `npm run build --workspace @corvus/ai` | `tsc`, exit 0; dist hash unchanged (not stale) | 0 |
| 5 | Format, touched files | `npx prettier --check <both files>` | "All matched files use Prettier code style!" | 0 |
| 6 | Root typecheck | `npm run typecheck` | all 5 workspaces clean | 0 |
| 7 | Root lint | `npm run lint` (`eslint . --max-warnings 0`) | clean | 0 |
| 8 | Consumer of served dist | `npm run test --workspace @corvus/web -- app/api/chat/route.test.ts` | **35 passed** | 0 |

### 8 (cont.) — the consumer check re-run against the rebuilt dist
This is the check that matters for "the served artifact carries the new prompt": the chat route
imports `buildPersonaPrompt` from `@corvus/ai` (i.e. dist) at `apps/web/app/api/chat/route.ts:25`
and asserts byte-equality of the composed system message at `route.test.ts:357`. 35/35 green after
my rebuild.

### Served-artifact string probe (runtime, not grep)
Imported the bare specifier and inspected the **emitted** prompt:

- Ask-line strings — **all 7 absent**, including the diacritic variants the spec names separately:
  `Can I start? Reply yes to build.` · `Baslayayim mi? Baslamak icin evet yaz.` · `Baslayayim mi?`
  · `Baslayalim mi?` · `Başlayayım mı?` · `Başlayalım mı?` · `yazman yeterli, ben baslatiyorum`.
- Residual acceptance-word list in the persona prompt — **absent**: `tamamdir`, `sen karar ver`,
  `evet`, `basla`, `you decide`, `signals acceptance`, `AGAIN`. (Words remain only in the *verdict*
  prompt's judging guidance, which is where the design intends them, and only for Turkish.)
- The two new lines — **present, byte-exact** (see Spec Adherence §2 for the em dash).
- Closing never-claim line, honesty and identity lines — all present.
- Verdict tri-state shape `{"verdict":"yes"|"no"|"unclear"}` + `EXACTLY one JSON object` — present
  for **both** languages. Turkish guidance present for `turkish`, **absent** for `english`. Turkish
  brief guidance present. Matches the spec's keep-list.
- Billed size: 17 emitted lines without `botName`, 18 with (was 19) — under the ≤20 cap the suite
  asserts at `persona-prompt.test.ts:94-98`.

### Guard-break proof (LESSONS §1.8), run with a validated instrument
A guard is not a guard until you break the thing it guards and watch it fail. I re-injected the
deleted contract into a **copy** of the prompt and re-ran the real test file against it. Critically,
I validated the instrument first, because a green control proves nothing if the copy was never
actually under test — my first two attempts at this harness produced a false "guard silent" reading
because vitest's default `exclude` hides `node_modules` (a broken instrument that would have been
read as a clean pass). Final harness: a gitignored scratch dir inside the package, the real test
copied beside the injected module, `exclude: []`.

| Step | Injection | Result |
|---|---|---|
| Control | unmodified source | **suite PASSED** (harness reproduces the real suite) |
| Positive control | plan line removed | **suite FAILED** (the copy IS the code under test) |
| INJ 1 | `Can I start? Reply yes to build.` | **CAUGHT** |
| INJ 2 | `Baslayayim mi? Baslamak icin evet yaz.` | **CAUGHT** |
| INJ 3 | repost-on-acceptance rule | **CAUGHT** |
| INJ 4 | `yazman yeterli, ben baslatiyorum` | **CAUGHT** |
| INJ 5 | acceptance-word list in the persona prompt | **CAUGHT** |
| INJ 6 | reworded final-line rule | **CAUGHT** |

All six injections were caught, and the two controls behaved as required. The report's own
guard-break table names the same guards; my independent run reproduces the conclusion on a harness
whose validity is demonstrated rather than assumed.

### UI surface
**No UI surface exists for this task.** It changes a system-prompt string; nothing renders. There is
no `.design-src/` comparison and no screenshot to take — stated explicitly rather than skipped.

---

## Spec Adherence (§3c)

**1. The four ask-line deletions happened by content.** Diffing the emitted prompt line-set against
committed `HEAD` gives exactly **4 removed, 2 added, 15 kept byte-identical** — the four removed are
precisely the spec's enumerated four: the byte-exact English final-line rule, the Turkish accept
line, the repost-on-acceptance rule, and the `yazman yeterli, ben baslatiyorum` handoff sentence.
Deleted by name, not by line number; line-number drift is irrelevant to the result.

**2. The two-line replacement matches the spec's bytes — including the em dash.** This was the
report's Open Question 1, so I tested it at the byte level rather than by eye:

- I extracted the §3c replacement strings from the spec file itself and normalised only the
  spec's own prose line-wrapping.
- The spec authors a **genuine U+2014** character (`E2 80 94`), not a `\u2014` escape and not a
  hyphen. Hex tail of the spec's wait line is `…20696e207468656972206f776e20776f72647320e2809420…`.
- `src/persona-prompt.ts` contains the spec line **byte-for-byte identical** (`includes()` on the
  extracted string returns true, and the matched slice re-serialises to the same hex).
- The served `dist/persona-prompt.js` also carries the **literal** character, not an escape form.

**Conclusion, stated explicitly as requested: the emitted bytes match the spec exactly.** The
report's Assumption 1 correctly flagged this for confirmation; it is confirmed, and **no edit is
needed**. The file's "ASCII-only" convention note is scoped to *diacritics in copy* and holds —
U+2014 is the file's only non-ASCII codepoint both before and after this task (verified: HEAD and
worktree each contain exactly one distinct non-ASCII character class, the em dash).

**3. Kept-identical items are untouched.** Verified against committed `HEAD`, not against the
report:

| Item | Result |
|---|---|
| 15 prompt lines (honesty / identity / voice / slash / non-coder / language) | **byte-identical** to HEAD |
| Closing `Chat text only describes: never claim a build started…` | byte-identical |
| `TURKISH_BRIEF_GUIDANCE` | byte-identical |
| `buildVerdictPrompt` body | byte-identical |
| `buildBriefPrompt` body | byte-identical |
| `OwnerLanguage` type | byte-identical |
| `TURKISH_VERDICT_GUIDANCE` | **differs from HEAD — see Finding F1; not this task's change** |

**4. The header comment no longer documents the deleted contract.** The 10-line comment was rewritten
to a 13-line intent-based version. It contains **none** of `Can I start`, `reply yes to build`,
`Baslayayim`, `Baslayalim`, `yazman yeterli`. It mentions "a final accept line" and "no ask line in
any language" only in **past tense, describing the deletion** — which is correct documentation, not a
surviving contract. (The single `accept line` hit in dist is this comment, not an emitted string.)

**5. Sibling redness is not caused by this task.** During my review the root typecheck was red with
two errors, both in **sibling-owned** `apps/web/app/dashboard/new/page.test.tsx(1155,47)` and
`(1159,49)`: `Cannot find name 'reaskAck'` — a fixture constant referenced but not defined, in a file
with mtime 16:12:24 (newpage-c, mid-edit). Zero errors in any `packages/ai` file, and
`npm run typecheck --workspace @corvus/ai` was exit 0 throughout. By the end of my review the sibling
work had landed and **root typecheck is exit 0** and root lint exit 0. Per instructions, I did **not**
fail this task for sibling files.

---

## Code Quality

- **No secrets or hardcoded credentials.** Regex scan of both touched files for `sk-…`, `xai-…`,
  `AIza…`, `ghp_…`, `Bearer <token>`, and `key|secret|password|token = "<8+ chars>"` → **clean**.
  The spec's prohibition on reading the secrets file was respected; nothing was read from it.
- **No manifest edits, no installs.** Confirmed by clean `git status` on all four manifest paths.
- **Scope respected.** This task created/modified exactly `packages/ai/src/persona-prompt.ts`,
  `packages/ai/src/persona-prompt.test.ts`, and the regenerated `dist` output. It touched no sibling
  file. The sibling-owned stale ask-line references the report escalated (Open Questions 3 and 4) are
  **flagged, not failed here** — they belong to verdict-b / newpage-c.
- **Test changes are guards, not leaks.** The 4 ask-line literals remaining in
  `persona-prompt.test.ts` are inside a negative-assertion block (`expect(prompt).not.toContain(…)`)
  plus one explanatory comment — correctly inverted. The suite also asserts the **set size**
  (`expect(removed).toHaveLength(5)`) alongside membership, which is the LESSONS §1.8 requirement.

---

## Findings

**F1 — `TURKISH_VERDICT_GUIDANCE` differs from committed HEAD, but predates this task.**
`packages/ai/src/persona-prompt.ts:62` · severity: **informational (attribution only, not a defect)**
The old guidance ("a Turkish yes is a yes: evet, tamam, tamamdir…") is replaced by intent-based
judging guidance ("judge APPROVAL INTENT in the reply's own language, not exact words…"). This is a
real diff against HEAD, so I verified attribution rather than taking the report's word for it. Three
independent lines of evidence say it is **not** this task's change:
(a) the report declares it as prior work, explicitly preserved as found and outside this diff's intent;
(b) the spec's own §3c instructs "KEEP identical: `TURKISH_VERDICT_GUIDANCE`" **while** §2 locks a
no-magic-words, intent-based design — so at spec-writing time the author must already have been
looking at the *new* guidance, since "keep identical" against the HEAD text would have re-locked the
very word list the design removes;
(c) a prior agent report in the same wave is specifically titled `…_verdict-guidance` and predates the
spec by ~3.5 hours.
The content itself is **correct and required** by the intent-based design, and is asserted by the new
tests. **Action: none for persona-a.** The orchestrator should know this change will ride along in
this wave's commit, since it is uncommitted.

**F2 — Stale ask-line references survive in sibling-owned files.** severity: **low, out of scope**
`apps/web/app/api/builder/verdict/route.test.ts:36` still carries a comment quoting
`Can I start? Reply yes to build.` and "its Turkish twins". Sibling files were still being written
during my review (mtimes 16:06–16:22), so this may already be resolved; `route.ts` and `page.tsx`
had **zero** ask-line/fold/`isPlanAsk` matches by my final sweep. Flagged, not failed — owner is
verdict-b / newpage-c. This matters because a stale comment is how the next agent re-learns a deleted
contract and rebuilds the loop, which is exactly the defect class this wave exists to kill.

**F3 — Reviewer-introduced contamination, disclosed.** severity: **informational**
My scratch dir briefly lived at `packages/ai/node_modules/.review-scratch/`, which updated that
directory's mtime to 16:17. It has been removed and no tracked file changed, but it means the
report's node_modules-mtime evidence is no longer re-verifiable. Manifest cleanliness stands on its
own.

No blocking findings. No secrets, no manifest edits, no scope violations, no spec deviations in the
task's own contract.

---

## Open Issues for Orchestrator

1. **Confirm F1's attribution is expected.** The guidance rewrite is correct but is a second agent's
   uncommitted work sharing this file and this commit. Confirm it was intended to land in this wave.
2. **The merged-tree live probe is still yours.** Per spec §7, "done" = the flow completed in the
   running app. This task proves the served artifact carries the new prompt and that the live
   consumer composes it (35/35). It does **not** prove the end-to-end path — that needs
   `verdict-b` and `newpage-c` to land. The report claims this correctly and does not overclaim.
3. **`dist/` is gitignored** (`.gitignore:2`), so the rebuilt output is a local artifact. The
   committed `src` is correct and `npm run ci` rebuilds before gating — but whoever deploys must run
   `npm run build --workspace @corvus/ai`, or the served prompt silently reverts.
4. **Sibling gate status at review time:** root typecheck and root lint were **both exit 0** at the
   end of my review (they were transiently red on sibling files mid-wave). Re-run the full gates on
   the merged tree yourself once all three agents report (LESSONS §6.1).
