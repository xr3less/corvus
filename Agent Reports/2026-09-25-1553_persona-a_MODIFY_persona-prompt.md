# Task Report: persona-a-intent-start

## Status
SUCCESS

## Files Touched
- MODIFIED: packages/ai/src/persona-prompt.ts
- MODIFIED: packages/ai/src/persona-prompt.test.ts
- MODIFIED (build output, regenerated by `tsc` only — never hand-edited): packages/ai/dist/persona-prompt.js, packages/ai/dist/persona-prompt.js.map, packages/ai/dist/persona-prompt.d.ts, packages/ai/dist/persona-prompt.d.ts.map, packages/ai/dist/persona-prompt.test.js, packages/ai/dist/persona-prompt.test.js.map, plus the rest of packages/ai/dist refreshed by the same `tsc` run

## Dependencies Added
None. No manifest edited, no install command run (verified: `git status` clean on `package.json` / `package-lock.json` / `packages/ai/package.json`; `packages/ai/node_modules` mtime unchanged at 2026-09-15).

## Assumptions Made
1. **The em dash in the spec's §3c replacement string is copy, not a typo.** The spec authored the sentence with `—` and this package's convention uses `—` throughout (6 pre-existing occurrences in `persona-prompt.ts`, plus `budget.ts`, `lanes.ts`, `router.ts`, `cost.ts`, every test file), so the em dash is the house style rather than an accident. The bytes were emitted ASCII-identical to the spec text (`their own words — any clear approval`). The file's "ASCII-only" note is scoped by its own wording to *diacritics in copy* (no Turkish `ç/ğ/ı/ş/ö/ü`), which holds — the file has exactly one non-ASCII character class, the em dash, as it did before this task. **If the orchestrator reads §3c's em dash as a transcription artifact, the fix is one whitespace-only edit in line 46 and I can re-run in a minute — flag it, do not silently reword.**
2. **The two "keep :28-41 and :46 as-is" line citations were stale** (the header comment block grew from 10 to 13 lines in the uncommitted F3 edit already on disk, shifting everything by ~3). I resolved by *content*, not by line number: kept every honesty/identity/voice line and the closing `never claim a build started` line, and deleted exactly the four ask-line lines the spec enumerated by name.
3. **"Verbatim ask-line assertions" in the test = the assertions that pinned the ask line as still-present.** I removed all six of those (two byte-exact `toContain` ask-line pins, the `signals acceptance` re-ask pin, the Turkish acceptance-signal pin, the handoff-sentence pin, and the two-copies-of-the-ask-line count pin) and replaced them with the inverse guards the spec asked for. The `describe` block was renamed `propose-then-ask (B-option C1)` → `plan-offer then wait (intent-based start, 2026-09-25)`.
4. **The rebuilt `dist/*.test.js` legitimately carries the removed strings** — inside the negative assertions (`expect(prompt).not.toContain('Can I start? Reply yes to build.')`) and the `removed` array. That is the guard, not a leak; `dist/persona-prompt.js` (the served artifact, and the only one `@corvus/ai` resolves) contains none of them. Confirmed by runtime probe, not by grep alone.
5. **Root `npm run typecheck` currently fails on 4 errors in `apps/web`, all outside my scope** — `app/api/builder/verdict/route.ts(244,18)` `foldAskText`, `(601,21)` `askedToStart`, and `app/dashboard/new/page.tsx(100,10)` `ASK_LINES` + its implicit-any parameter. Those are the sibling agents' in-flight deletions (verdict-b / newpage-c) mid-edit. I did **not** touch them and did not attempt a fix (scope guard). `npm run typecheck --workspace @corvus/ai` passes clean.
6. **Root `npm run lint` flipped from exit 0 to exit 1 during this task, in a sibling file.** At 15:59 it was clean; at 16:04 it reports a single parsing error — `apps/web/app/api/builder/verdict/route.test.ts:729` `Parsing error: Declaration or statement expected` (file mtime 16:04:10, actively being written by verdict-b). It was not clean-then-broken by anything I did: my two files lint clean on their own (`exit 0`, re-confirmed), and I never touched that file. **Flagging so the merged-tree lint failure is not attributed to this task** — but it also means the wave cannot pass the spec §7 gate until verdict-b's test file parses. Worth checking that verdict-b's write actually completed rather than being truncated mid-edit.

## Open Questions for Orchestrator
1. **Em dash in §3c** — confirm the `—` in the new "Then wait…" line is intended copy (Assumption 1). Everything else is byte-faithful to the spec.
2. **`apps/web` typecheck is red while the wave is in flight.** My package is green in isolation, but the merged tree will not typecheck until verdict-b and newpage-c land their deletions. You own the merged-tree gate (spec §7) — flagging so the red does not get attributed to this task.
3. **Two byte-exact ask-line constants still live outside my scope and must die in the sibling tasks**, or the class-fix is incomplete (LESSONS §3 — both gates go together, never one call site):
   - `apps/web/app/api/builder/verdict/route.ts:144` — header comment quoting the ask line verbatim.
   - `apps/web/app/dashboard/new/page.tsx:65` and `:70` — header comment quoting the ask line and `Baslayayim mi?`.
   These are comments, so they are harmless at runtime, but the spec's own §3a/§3b require both header comments to be rewritten anyway. Noting it because a stale comment is how the *next* agent re-learns a deleted contract and rebuilds the loop.
4. **`apps/web/app/api/builder/verdict/route.test.ts:28,34` and `apps/web/app/dashboard/new/page.test.tsx:88`** still define `ASK` / `ASK_TR_ASCII` / `ASK_LINE_TR_ASCII` constants. Per §3b/§3a those files are owned by the sibling agents, so I left them alone — but they are the fixtures most likely to keep the deleted contract alive in the test suite. Worth an explicit check in the review gate.

## Public Interface Exposed
Signature surface byte-identical to before this task (verified against the rebuilt `dist/persona-prompt.d.ts`):

```ts
export type OwnerLanguage = 'english' | 'turkish';
export declare function buildPersonaPrompt(opts?: { botName?: string }): string;
export declare function buildVerdictPrompt(planText: string, userReply: string, language?: OwnerLanguage): string;
export declare function buildBriefPrompt(threadText: string, language?: OwnerLanguage): string;
```

`buildPersonaPrompt` emitted-line surface (the only behavioral change): two lines replace four.

Removed (never emitted again, in any language): the English byte-exact final-line rule, the Turkish accept line `Baslayayim mi? Baslamak icin evet yaz.`, the repost-on-acceptance rule, the `yazman yeterli, ben baslatiyorum` handoff sentence.

Kept verbatim: `After at most 2-3 short questions, post a 2-4 bullet plan summary in the owner's language.` and the closing `Chat text only describes: never claim a build started, is running, or is done.`

Added: `Then wait for the owner's reply in their own words — any clear approval starts the build from the page; you never start, run, or claim any build yourself.`

Prompt is now **17 lines** without `botName` (was 19) — under the ≤20 billing cap, asserted with and without `botName`.

`TURKISH_VERDICT_GUIDANCE`, `TURKISH_BRIEF_GUIDANCE`, and both builders' behavior untouched by this task (the one uncommitted change already on disk in `TURKISH_VERDICT_GUIDANCE` is a prior agent's work, preserved as found and not part of this diff's intent).

## Verification Run (commands + observed results)

| Check | Command | Result |
|---|---|---|
| Baseline before edit | `npm run test --workspace @corvus/ai` | 158 passed (6 files) |
| Package tests | `npm run test --workspace @corvus/ai` | **161 passed** (6 files) — `persona-prompt.test.ts` 42→45 |
| Package typecheck | `npm run typecheck --workspace @corvus/ai` | exit 0, zero errors |
| Root lint | `npm run lint` (`eslint . --max-warnings 0`) | exit 0, zero warnings at 15:59 — **see note below: it later flipped to exit 1 in a sibling file** |
| Scope lint | `npx eslint packages/ai/src/persona-prompt.ts packages/ai/src/persona-prompt.test.ts --max-warnings 0` | exit 0, zero warnings (re-confirmed 16:04) |
| Package format | `npm run format --workspace @corvus/ai` | "All matched files use Prettier code style!" |
| Touched-file format | `npx prettier --check packages/ai/src/persona-prompt.ts packages/ai/src/persona-prompt.test.ts` | clean (one reformat applied to the test file) |
| Build | `npm run build --workspace @corvus/ai` (`tsc`) | exit 0 |
| **Served artifact (runtime probe, not grep)** | `node --input-type=module -e "import {...} from '@corvus/ai'"` | ask-line occurrences in the served prompt: **0**; the four required new fragments present; `{"verdict":"yes"\|"no"\|"unclear"}` present; Turkish verdict guidance present, absent for English; Turkish brief guidance present |
| Dist ask-line scan | `grep` over `packages/ai/dist/persona-prompt.js` | all 9 removed strings **absent** |
| Consumer (reads served dist) | `npm run test --workspace @corvus/web -- app/api/chat/route.test.ts` | **35 passed** — the chat route composes the new prompt and its byte-equality assertions hold |

### Guard-break proof (LESSONS §1.8 — a guard is not a guard until you break the thing and watch it fail)

I re-injected the deleted contract one piece at a time into `buildPersonaPrompt` and re-ran the suite. Every new guard failed, and only the intended ones:

| Injected | Guard that fired |
|---|---|
| `Can I start? Reply yes to build.` | `carries no ask-line contract in any language` |
| the repost rule + `final line is always this exact line` + the handoff sentence | `re-asks nothing on an acceptance signal` (+ the ask-line guard, + the ≤20-line cap) |
| `tamamdir` and `sen karar ver` as an acceptance-word list | `carries no acceptance-word list: the judge weighs words, not the chat` |

The file was then restored from a copy kept **outside the repo** (`/tmp/corvus-persona-baseline/`) — no `git stash`/`checkout --`/`restore`/`reset` was run at any point while this wave is uncommitted (LESSONS §6.1).

## Known Limitations
- **Did not exercise the end-to-end flow in the running app.** This is the prompt-builder half of a three-part change; the live probe the spec asks for (Turkish paraphrase approval → 200 `verdict: yes, phase: queued` + a `builder_runs` row) cannot pass until verdict-b and newpage-c land, because the route and page gates this task's prompt was feeding are being deleted by them. What I *did* prove on the real path is that the served `@corvus/ai` artifact carries the new prompt and the live consumer (`app/api/chat/route.ts`) still composes and asserts against it byte-for-byte. Per spec §7, the merged-tree live probe is the orchestrator's gate, not mine — this report does not claim it.
- **No UI surface to screenshot** — this task changes a system prompt string, no rendered output.
- Stale ask-line references remain in the two sibling-owned files and two sibling-owned test fixtures (Open Questions 3 and 4). Out of scope; escalating rather than expanding silently.
- `packages/ai/dist` is gitignored, so the rebuilt output is a local artifact — whoever deploys must run `npm run build --workspace @corvus/ai` (already part of root `npm run ci`). The committed `src` is the source of truth and is correct.
