# SPEC Map: site-to-engine path and the site↔live-execution gap
Status: SUCCESS SPEC — site mints/versions/publishes opaque spec pointers; gateway boots workers but zero bots log in, so no site action reaches Discord.
Touched: CREATED this SPEC (read-only; cited web/gateway/testbot/spec files with lines).
Note: Testbot holds 9 live capabilities gateway lacks; 6 seam questions remain (translator, tokens, registration, boot reader, state, parity).
