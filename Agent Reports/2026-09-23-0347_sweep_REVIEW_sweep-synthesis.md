# Task Report: sweep-synth-0347

## Status
SUCCESS

## Files Touched
- CREATED: Agent Reports/2026-09-23-0347_sweep_REVIEW_sweep-synthesis.md

## Dependencies Added
None.

## Assumptions Made
- The 12 journal area results plus the 0339 ci-tests file constitute the complete 13/13 raw set; the duplicate `started` entry for inspect:builder-api is a retry marker, not a gap (only one builder-api result exists, as instructed).
- Fix verification is read-only against the working tree at HEAD `d9cf8d7`; a fix counts as FIXED only when the correcting code is present on disk (file:line cited), never on the orchestrator's say-so.
- Severity rubric applied: blocker = data loss, dishonest product surface, or money leak; major = broken user-visible flow; minor = wart with workaround; nit = polish. Inspector severities kept unless their own evidence contradicts them (overrides, where they occur, are called out).

## Open Questions for Orchestrator
- B-1 (landing hero mockup) and M-1..M-4 (landing honesty defects) are product/business decisions as much as engineering: replacing the fabricated screenshot with a real one (or labeling it a mockup), renaming or seeding the three showcased templates, building or removing the data-export promise, and obtaining a real Discord invite all need founder direction. Recommend presenting these as business choices before assigning fix work.
- M-10 (rollback target derived from draft head) needs a design decision: derive the target from the prod pointer (strictly-older published) instead of `draftVersion - 1`, or change the route contract. The spec/contract is the source of truth here; whichever side the contract blesses, the other side must move.
- M-7/M-8/M-9 (new-page mint/judged/runId latches) are three facets of one broken recovery story on `apps/web/app/dashboard/new/page.tsx`. Recommend one atomic fix task covering all three resets rather than three separate tasks.
- The XP in-memory wipe (m-36) is scored minor per the inspector, but it is restart data loss of user-visible state; confirm minor is acceptable or escalate to major.
- I did not touch the verdict trio, refusal-unify files, or any CSS per the scope guard (sibling agent owns them). Several OPEN items live in exactly those files and must be sequenced after the sibling lands.

## Public Interface Exposed
None.

## Known Limitations
- Verification is static (source reads + `git status/log`); I did not boot the app or run suites. FIXED means "correcting code verified present on disk," not "proven in the running app."
- Landing findings were judged from inspector evidence plus targeted greps; I did not re-screenshot the landing page.
- No web research was needed (all facts repo-internal); citations are file:line.

---

# Sweep synthesis: judged list (13/13 areas)

Raw input: 114 findings (104 journal + 10 ci-tests). After merging 10 multi-reported defects: **102 unique** — 1 OPEN blocker, 13 OPEN majors, 56 OPEN minors, 19 OPEN nits, 13 FIXED (verified on disk), 0 dropped.

## Ranked defects — OPEN first

### Blocker (1)

- **B-1 blocker [OPEN]** `apps/web/app/page.tsx:255` — The hero "product screenshot" is a fabricated mockup (invented analytics, fake event logs, fake user, fake URL) presented under a pulsing "live" dot with alt text claiming "Real-time," while the real dashboard renders honest empty states; unlike every other forward-looking claim on the page it carries no mockup/planned label. Evidence: journal landing finding (lines 244-258, image inventory vs KI-030 empty states). Areas: landing.

### Majors (13)

- **M-1 major [OPEN]** `apps/web/app/page.tsx:540` — Template showcase advertises "Community Guardian / AI Support Desk / Welcome & Role Picker" with "Use this template" buttons, but the seeded catalog holds 8 differently-named templates and the promised capabilities (button-roles, rules-gate, AI answers) do not exist in the seeds. Evidence: journal landing finding (seed-templates.ts:48-347 vs cards at 540/589/638). Areas: landing.
- **M-2 major [OPEN]** `apps/web/app/page.tsx:293` — "Your data and bot come with you if you leave" (echoed by privacy :151-152 and terms :49) has no backing feature: zero export/download UI in `dashboard/**` and no route emitting an attachment. Evidence: journal landing finding (grep-negative). Areas: landing.
- **M-3 major [OPEN]** `apps/web/app/page.tsx:213` + `apps/web/app/page.tsx:737` (merged with auth-trial `apps/web/app/page.tsx:213`) — Primary CTA "Start building free" sends anonymous visitors to `/dashboard` with no auth gate (no middleware, client-shell layout); first submit 401s and shows "Could not save your bot … try again," which can never succeed, with no login affordance on the page. Inspector severities (major landing / minor auth-trial) merged at major: the user-visible flow is broken end to end. Evidence: both areas' findings (bots/route.ts:83, new/page.tsx:28/192). Areas: landing, auth-trial.
- **M-4 major [OPEN]** `apps/web/app/page.tsx:732` + `:950-951` — Starter tier promises "Community Discord support" while the footer's "Community Discord"/"X (Twitter)" entries are dead non-link spans (same hover styling as real links) and no invite URL exists anywhere in the web app. Evidence: journal landing finding. Areas: landing.
- **M-5 major [OPEN]** `apps/web/app/api/auth/callback/route.ts:24` + `apps/web/app/api/auth/login/route.ts:16` (merged with landing login-bounce finding) — OAuth/code failures redirect to `/?error=<code>` but no page ever reads the param (only runId/view/tab consumers exist; `app/page.tsx` has no searchParams use — grep-verified), so a failed sign-in silently lands on an identical-looking page. Inspector severities (major auth-trial / minor landing) merged at major. Evidence: both areas' findings. Areas: auth-trial, landing.
- **M-6 major [OPEN]** image-attachment dead path (merged: bot-detail `apps/web/app/dashboard/bots/[id]/page.tsx:837` minor + dashboard-home-bots same-file major + new-chat `apps/web/components/ui/ai-chat-input.tsx:615` major) — Files are thumbnailed/counted ("1 attachment(s)") but never transmitted: `use-chat-stream.ts:80` posts only `{botId, message, history}`, no attachment field exists in chat validation, and image-only submits clear silently. Judged major (broken affordance, majority severity): the UI promises an upload nothing consumes. Evidence: all three areas' findings (use-chat-stream.ts:120-147 verified: `attachmentCount` stored, body has no attachments). Areas: bot-detail, dashboard-home-bots, new-chat.
- **M-7 major [OPEN]** `apps/web/app/dashboard/new/page.tsx:216` — Failed mint is terminal yet invites retry: `mintAttemptedRef` is set once (lines 215-217, no reset verified on disk) so after `mintOnce` fails, `botId` stays null forever, the verdict gate (`botId === null → return`, line 115) never opens, and "Your chat is kept — try again" describes a remedy the guard makes impossible. Evidence: journal new-chat finding, re-verified in current source. Areas: new-chat.
- **M-8 major [OPEN]** `apps/web/app/dashboard/new/page.tsx:121` — Verdict transport failure pins `judgedUserIdRef` before the POST resolves (line 117 sets, line 112 guards, catch at 166-168 never clears), so the next turn's preceding row is the ack rather than the ask line and "Could not start the build. Try again." has no working retry. Evidence: journal new-chat finding, re-verified in current source. Areas: new-chat.
- **M-9 major [OPEN]** `apps/web/app/dashboard/new/page.tsx:105` (merged with builder-api `apps/web/app/dashboard/new/page.tsx:287` short-circuit) — "A yes after a failed build starts a fresh run" (line 286) is unimplementable: `runId` is set once (line 162) and never reset (grep-verified: no `setRunId(null)` path), so the `runId !== null → return` guard blocks every verdict POST after the first build. Duplicate merged, single defect. Evidence: both areas' findings, re-verified. Areas: new-chat, builder-api.
- **M-10 major [OPEN]** `apps/web/app/dashboard/bots/[id]/page.tsx:526` — Rollback derives its target from the DRAFT head (`draftVersion - 1`, lines 559-564 verified still present) but `/api/spec/rollback` only accepts previously-published versions strictly older than prod (`target.version >= current.version → 404`, rollback/route.ts:222-234 verified), so rollback after any draft-only save 404s and the copy falsely claims "This bot is not saved on the server yet." Evidence: journal bot-detail finding, re-verified both sides. Areas: bot-detail.
- **M-11 major [OPEN]** `apps/web/app/api/chat/route.ts:378` — Turns ending without a `done` frame (client abort via `req.signal`, mid-stream provider throw converted to an error frame at :372-377) incur real provider cost but write no `ai_spend` row, so the monthly-allowance SUM (:124-126) silently undercounts and the "refuse a turn that would cross the grant" gate erodes; route.test.ts:326-345 locks the no-write behavior in. Judged major (bounded per-turn leak + gate erosion, not unbounded runaway). Evidence: journal chat-api finding, re-verified (`if (done)` at :378, abort path in stream.ts). Areas: chat-api.
- **M-12 major [OPEN]** `apps/web/app/api/interview/interview.test.ts:289` — CI gate deterministically red on master: the KI-033 one-bot cap (`mintGate`, bots.ts:111) collides with the suite's single shared `owner` account, so every `startBot` after the first mints 403 (confirmed by fixwave-batch reviewer: interview 3 + templates 2 failures). Evidence: ci-tests 0339 file. Areas: ci-tests.
- **M-13 major [OPEN]** `infra/RUNBOOK.md:155` — No migration runner and no applied-migrations journal: CI boots an empty DB and every suite hand-applies a different ad-hoc subset with swallowed failures; reviewer proved a pristine DB goes from 3 to 8 failures. Failure count is a function of database state. Evidence: ci-tests 0339 file. Areas: ci-tests.

### Minors (56)

- **m-1 minor [OPEN]** `apps/web/app/dashboard/new/page.tsx:52` (merged auth-trial + dashboard-home-bots + builder-api `:141`) — `readRefusalMessage` falls back to the raw `error` string, so code-only shapes (401 `{error:'unauthorized'}`, 422 `empty_brief`, "body must be JSON") render verbatim in `role=alert` copy. The message-preferred half is fixed (shared readers); the code-only tail is not. Evidence: three areas' findings; bounds.ts:69-70 + refusal.ts:34 verified. Areas: auth-trial, dashboard-home-bots, builder-api.
- **m-2 minor [OPEN]** `apps/web/app/dashboard/bots/page.tsx:78` (merged auth-trial + dashboard-home-bots) — List ignores `snapshot.unauthorized`: a 401 renders "No bots yet — describe your first bot." instead of the logged-out line + login link the `[id]` page shows. Verified still present (`liveSnapshot?.bots ?? []`, no unauthorized read). Areas: auth-trial, dashboard-home-bots.
- **m-3 minor [OPEN]** `apps/web/app/dashboard/bots/page.tsx:1` — List never fetches `/api/session/trial`: expired-trial users get no pause banner on the one surface listing their paused bots, while home and `[id]` both show it. Evidence: journal dashboard-home-bots finding. Areas: dashboard-home-bots.
- **m-4 minor [OPEN]** `apps/web/app/dashboard/bots/page.tsx:78` + `apps/web/app/dashboard/page.tsx:58-71` — No loading state on home/bots list: `liveSnapshot` starts null so the full "No bots yet" empty state flashes before the fetch resolves (detail page holds the shell with `list.status === 'loading'`). Evidence: journal dashboard-home-bots finding. Areas: dashboard-home-bots.
- **m-5 minor [OPEN]** `apps/web/app/gallery/page.tsx:309` — No-match empty state doubles as loading state: initial `templates: []` falls into `visible.length === 0` before the fetch resolves, flashing "No templates match that search." plus a no-op Clear button. Verified still present (lines 109-110, 305-315). Areas: gallery.
- **m-6 minor [OPEN]** `apps/web/app/gallery/page.tsx:306` — Unavailable copy says "try again" but offers no retry control; manual reload is the only path. Evidence: journal gallery finding. Areas: gallery.
- **m-7 minor [OPEN]** `apps/web/app/dashboard/page.tsx:231` — Hardcoded "Workspace: My server" and setup step "Connect your server — Done" (plus "Two steps done", aria-valuenow=2) present fabricated account state as real, against the KI-030 rule the same file enforces. Evidence: journal dashboard-home-bots finding. Areas: dashboard-home-bots.
- **m-8 minor [OPEN]** `apps/web/app/dashboard/page.tsx:254` — Three template preset cards are enabled buttons with no onClick (code comment admits it), dead UI without the Coming-soon treatment the adjacent Upgrade button uses. Evidence: journal dashboard-home-bots finding. Areas: dashboard-home-bots.
- **m-9 minor [OPEN]** `apps/web/app/dashboard/page.tsx:52` — `?view=bots` redirect drops all other params, silently discarding `?runId=` on legacy links so Build progress shows "No run started." Evidence: journal dashboard-home-bots finding. Areas: dashboard-home-bots.
- **m-10 minor [OPEN]** `apps/web/components/ui/builder-progress.tsx:57` — `readErrorMessage` returns raw browser text ("Failed to fetch") into the `role=alert` on network failure. Evidence: journal dashboard-home-bots finding. Areas: dashboard-home-bots.
- **m-11 minor [OPEN]** `apps/web/app/dashboard/page.tsx:232` — Workspace strip renders `{TRIAL_DEAL}` unconditionally; a paid-tier account would read "Free 3-day trial — 1 bot, 100 AI credits" (detail page gates the same constant on `status === 'trial'`). Evidence: journal dashboard-home-bots finding. Areas: dashboard-home-bots.
- **m-12 minor [OPEN]** `apps/web/components/ui/ai-chat-input.tsx:549` — Attachment blob-URL revocation effect re-runs on every attachments-array change, revoking URLs still in the array and breaking the preview modal for earlier attachments after a second image is added. Evidence: journal dashboard-home-bots finding. Areas: dashboard-home-bots.
- **m-13 minor [OPEN]** `apps/web/app/dashboard/new/page.tsx:270` — Streaming thread has no live region (`<ul aria-label="New bot conversation">`, no aria-live; "Saving your bot…" has no role=status) while the demo page announces politely. Evidence: journal new-chat finding. Areas: new-chat.
- **m-14 minor [OPEN]** `apps/web/components/ui/chat-thread.module.css:122` — `.composerNoRing :focus-visible { outline:none; box-shadow:none }` covers every focusable descendant (chips, attach/send buttons), not just the textarea; the focus-within border shift does not identify which control holds focus. Evidence: journal new-chat finding. Areas: new-chat.
- **m-15 minor [OPEN]** `apps/web/app/dashboard/new/page.tsx:269` — No auto-scroll on new turns: replies and Build progress land below the fold in `.newScroll`; page.test.tsx still stubs scrollIntoView the page never calls. Evidence: journal new-chat finding. Areas: new-chat.
- **m-16 minor [OPEN]** `apps/web/app/dashboard/new/page.tsx:227` — `showSaving` ignores `mintError`, so "Saving your bot…" renders beside the refusal alert indefinitely after a failed mint. Evidence: journal new-chat finding. Areas: new-chat.
- **m-17 minor [OPEN]** `apps/web/components/ui/ai-chat-input.tsx:788` — Enter during IME composition submits mid-conversion (no `isComposing`/keyCode-229 check), firing sends for non-Latin input while composing. Evidence: journal new-chat finding. Areas: new-chat.
- **m-18 minor [OPEN]** `packages/ai/src/budget.ts:90` — `resolveAllowance` indexes `MONTHLY_GRANTS[tier]` directly, so a prototype key ('toString') resolves to the inherited function, skips the throw, and returns `{ok:true}` via NaN comparison — contradicting the module's own doc (lines 36-37) and asymmetric with unknown tiers that throw. Latent (both callers pre-gate with `isPlanTier`, verified at verdict route :394 and chat route) but the exported boundary itself is unguarded. Evidence: journal ai-package finding; `isPlanTier` uses hasOwnProperty (line 39-40) while `resolveAllowance` does not. Areas: ai-package.
- **m-19 minor [OPEN]** `packages/ai/src/persona-prompt.ts:10` — Never-claim denylist names only Grok/xAI/Claude/Anthropic/OpenAI, none of which the persona lane routes to now (defaults GLM 5.2 / Z.AI / DeepSeek per lanes.ts:109-141); asked "are you GLM / made by Z.AI?" the prompt is silent. Verified line 10 unchanged. Areas: ai-package.
- **m-20 minor [OPEN]** `packages/ai/src/eval/golden-briefs.ts:76` — `extractFencedJson` is duplicated from `builder-runs.ts:330-334` with no lock test binding the copies; the eval's parity harness consumes its own copy, so gateway-side drift is undetectable. Evidence: journal ai-package finding. Areas: ai-package.
- **m-21 minor [OPEN]** `apps/web/app/api/builder/verdict/route.ts:313` — User-authored thread/plan/reply text is interpolated into the system role via buildVerdictPrompt/buildBriefPrompt (verified lines 434-443, 473-476), contradicting the D9 doctrine locked in builder-runs.ts:356-357 (brief stays a delimited user turn). Bounded impact (3-value verdict, 2000-char brief clamp) but a user can steer the model-written brief. Evidence: journal ai-package finding. Areas: ai-package.
- **m-22 minor [OPEN]** ghost-writable soft-deleted bots (merged db-migrations finding across 5 routes) — Ownership reads omit `deleted_at IS NULL` on all five write paths (interview/answer :90, spec/publish :419, spec/rollback :107 per route reads — verified `:107`-adjacent SELECT without the filter at rollback :169/publish :419/draft :60/simulate :168), while every list/poll/vault read filters; soft-deleted bots stay mintable/publishable/rollable-back/simulatable while invisible in UI. Evidence: journal db-migrations finding, re-verified. Areas: db-migrations.
- **m-23 minor [OPEN]** `apps/web/app/api/chat/route.ts:316` — Allowance gate is read-then-later-record with no reservation: two concurrent turns both read spent=99.0, both pass, account ends at ~100.8 against a 100 grant. Overflow bounded by in-flight count. Evidence: journal chat-api finding. Areas: chat-api.
- **m-24 minor [OPEN]** `apps/web/app/api/chat/route.ts:117` — Budget estimate prices ONE persona call but `chatStream` can fail over across three routes (stream.ts:305-362), so a failure-heavy turn spends ~2.7 credits against a 0.9 reservation (builder worker multiplies by remaining attempts; chat does not). Evidence: journal chat-api finding. Areas: chat-api.
- **m-25 minor [OPEN]** `apps/web/app/api/chat/route.ts:372` — Mid-stream catch swallows the thrown error with no server-side log (coded lane diagnostics lost); every other failure path in the route logs its cause. Evidence: journal chat-api finding. Areas: chat-api.
- **m-26 minor [OPEN]** `apps/web/app/api/builder/start/route.ts:199` (same knob at preflight/start :201) — `singletonKey: runId` is inert: queue created with no options → 'standard' policy with no singleton uniqueness index (verified pg-boss 12.30.0 per inspector); real retry idempotency comes from the `_builder` marker + spend ledger. Dead knob, correctness holds. Evidence: journal builder-api finding. Areas: builder-api.
- **m-27 minor [OPEN]** `apps/web/components/ui/builder-progress.tsx:80` — A single transient poll failure (catch at :80-82, !ok at :91-97 — verified both `return` without rescheduling) permanently stops polling with no retry; live progress never resumes without remount. Evidence: journal builder-api finding. Areas: builder-api.
- **m-28 minor [OPEN]** `apps/gateway/src/db/builder-runs.ts:625` — Worker's generate step makes its first billable `chat()` call before confirming the bot exists: the existence read (:595-608) only gates the budget, and the `bot_gone` check lands post-call (:652-656); a bot deleted after enqueue burns one unattributed, unbilled provider call. Evidence: journal builder-api finding. Areas: builder-api.
- **m-29 minor [OPEN]** `apps/gateway/src/runtime/connector/index.ts:226` — Poll loop fires `pollOnce` with no `.catch` and `applyReading(..., now())` at :151 sits outside the try (:128-141 verified), so a rejection becomes an unhandled rejection under Node defaults; sibling polls wrap their ticks. Evidence: journal gateway finding. Areas: gateway.
- **m-30 minor [OPEN]** `apps/gateway/src/runtime/connector/index.ts:232` — `timer.unref()` unguarded, unlike the typeof-guarded idiom in startGiveawayPoll/startTempbanPoll. Evidence: journal gateway finding. Areas: gateway.
- **m-31 minor [OPEN]** `apps/gateway/src/runtime/dispatcher.ts:70` — Cooldown stamped before `execute`, so a throwing command still consumes the window; `lastUsed` has no eviction and grows with distinct user/command pairs. Evidence: journal gateway finding. Areas: gateway.
- **m-32 minor [OPEN]** `apps/gateway/src/runtime/welcome/handler.ts:160` — `joinTimes` raid ring is module-scope shared across guilds: a join burst in one guild can trigger raid suppression for a different guild on the same bot. Evidence: journal gateway finding. Areas: gateway.
- **m-33 minor [OPEN]** `apps/gateway/src/runtime/loaders.ts:82` — Per-kind resolution takes the first row per kind with no guildId precedence and BOOT_CONFIG_SQL has no ORDER BY, though config.ts documents per-guild override; no read side implements it. Evidence: journal gateway finding. Areas: gateway.
- **m-34 minor [OPEN]** `apps/gateway/src/start.ts:346` — `restartBotFromVault`'s vault read filters only `isNull(bots.deletedAt)` (line 429 verified) without `status = 'live'`, so a paused/slept/quarantined bot is resurrected by the supervisor path (unlike BOOT_LIVE_BOTS_SQL at :313). Evidence: journal gateway finding. Areas: gateway.
- **m-35 minor [OPEN]** `apps/gateway/src/runtime/moderation/automod.ts:43` — `buildBadWordsRe` compiles a fresh RegExp per message on the hot MessageCreate path instead of once per config. Evidence: journal gateway finding. Areas: gateway.
- **m-36 minor [OPEN]** `apps/gateway/src/runtime/boot-modules.ts:29` — `buildXpModule()` starts with no store: production XP/balance/leaderboard live in InMemoryXpStore, wiped every restart, while a durable Postgres store exists unwired (and the sync port cannot accept the async store as-is). Evidence: journal gateway finding. Areas: gateway.
- **m-37 minor [OPEN]** `apps/web/app/dashboard/bots/[id]/page.tsx:406` — Pre-flight poll loop survives unmount: `scanActive` is cleared only in the `[bot]` effect body (line 440), which does not run on unmount (no cleanup return there, unlike the activity effect's AbortController at :449-452), so polling continues up to 60s after the page is gone. Evidence: journal bot-detail finding. Areas: bot-detail.
- **m-38 minor [OPEN]** `apps/web/app/dashboard/bots/[id]/page.tsx:437` — Activity feed conflates 401 with all failures ("check your connection") instead of the LOGGED_OUT_LINE + login link every write path on the page uses; the test at page.test.tsx:523-535 locks the misleading copy. Evidence: journal bot-detail finding. Areas: bot-detail.
- **m-39 minor [OPEN]** `apps/web/app/dashboard/bots/[id]/page.tsx:779` (merged with dashboard-home-bots nit) — `brief.length > 2000` guard is unreachable: `stitchBrief` clamps to BRIEF_MAX_CHARS=2000 first (thread.ts:118 verified shape), so long briefs truncate silently instead of hitting the honest error. Judged minor (dead-code-or-silent-truncation, merged up from nit). Evidence: both areas' findings. Areas: bot-detail, dashboard-home-bots.
- **m-40 minor [OPEN]** `apps/web/app/dashboard/bots/[id]/page.tsx:128` — RED pre-flight rows render with the same amber warn dot as yellow advisories (`toScanRow` collapses to pass/warn); only the summary sentence carries the distinction that blocks Save version. Evidence: journal bot-detail finding. Areas: bot-detail.
- **m-41 minor [OPEN]** `apps/web/app/dashboard/bots/[id]/page.tsx:992` — Detail tabs declare tablist/tab roles with no arrow-key navigation or roving tabindex, breaking the ARIA tabs contract while announcing it. Evidence: journal bot-detail finding. Areas: bot-detail.
- **m-42 minor [OPEN]** `apps/web/app/page.tsx:863` — FAQ accordion unsound for screen readers: buttons lack aria-controls and collapsed answers stay in the a11y tree (purely visual grid-rows collapse), making aria-expanded misleading. Evidence: journal landing finding. Areas: landing.
- **m-43 minor [OPEN]** `apps/web/app/landing-islands.tsx:200` — Anchor interceptor captures the "Skip to content" link and preventDefaults it, but `<main id="top">` is not focusable, so focus never moves and Tab re-enters header links. Evidence: journal landing finding. Areas: landing.
- **m-44 minor [OPEN]** `apps/web/app/landing.module.css:962` — `#71717a` on near-black surfaces computes ~4.0-4.3:1, below WCAG AA 4.5:1 for the 11-14px text it styles (panelLabel, timeSub, pricePer, priceNote, footerHeading). Evidence: journal landing finding. Areas: landing.
- **m-45 minor [OPEN]** `apps/web/app/landing.module.css:133` — Every `.reveal` ships at opacity 0, restored only by a client IntersectionObserver or the reduced-motion query; with JS disabled or failed hydration the H1 and all sections stay invisible. Evidence: journal landing finding. Areas: landing.
- **m-46 minor [OPEN]** `apps/web/app/page.tsx:256` — 579KB hero JPEG via plain eager `<img>` with no width/height, srcset, or next/image: LCP weight plus reflow on decode. Evidence: journal landing finding. Areas: landing.
- **m-47 minor [OPEN]** `apps/web/app/landing-islands.tsx:410` — WebGL hero shader (3-octave simplex per pixel, DPR ≤ 2) runs rAF forever with no IntersectionObserver pause after the hero scrolls off-screen. Evidence: journal landing finding. Areas: landing.
- **m-48 minor [OPEN]** `apps/web/app/page.tsx:32` — Nav "Architecture" targets `#bento`, the same section as "Features," whose content holds no architecture material. Evidence: journal landing finding. Areas: landing.
- **m-49 minor [OPEN]** `apps/web/app/page.tsx:439` — "Test on a practice server before going live" is promised as a user step, but the shipped surface is a fake-event draft simulator (`POST /api/simulate`, "No Discord calls"); the only real practice guild is internal, and terms :149 frames it as Corvus's own testing. Evidence: journal landing finding. Areas: landing.
- **m-50 minor [OPEN]** `apps/web/app/api/webhooks/creem/route.test.ts:1203` — Money-guard suite order-dependent under full-suite parallel load: inline FALLBACK_DDL is a no-op when `accounts` already exists via 0002 (no tier), and unlike siblings (activity :132-134) creem carries no `tier` self-heal → `column "tier" does not exist`. Passes isolated, reds in full runs. Evidence: ci-tests 0339 file. Areas: ci-tests.
- **m-51 minor [OPEN]** `apps/gateway/src/launch-blockers.test.ts:303` — Four real-PG tests call `ctx.skip()` with no `return`, so DB-less runs continue into live queries and burn each 60-120s cap (~6 wasted minutes); siblings do `skip(); return;` and the pool sets no `connectionTimeoutMillis`. Evidence: ci-tests 0339 file. Areas: ci-tests.
- **m-52 minor [OPEN]** `apps/web/lib/auth/session-db.test.ts:553` — ~11 web suites' "try real sibling migrations first" path is dead code under vitest (`new URL(relative, import.meta.url)` hits the dev-server origin, readFile rejects, empty catch routes to fallback DDL), so migration↔fallback drift is never exercised. Evidence: ci-tests 0339 file (11 files enumerated). Areas: ci-tests.
- **m-53 minor [OPEN]** `package.json:18` — Workspace `test` gate is latent silent-skip: `--if-present` passes over any workspace lacking a `test` script (LESSONS L-009 class); same shape on `typecheck`/`format`. Evidence: ci-tests 0339 file. Areas: ci-tests.
- **m-54 minor [OPEN]** `package.json:18` — `npm test --workspaces` runs sequentially and aborts remaining workspaces on first failure: a red gateway run means testbot/web suites silently never execute that CI run. Evidence: ci-tests 0339 file. Areas: ci-tests.
- **m-55 minor [OPEN]** `apps/web/app/dashboard/new/page.module.css:107` — Safe-area pad is dead and wipes the designed 20px composer bottom padding: `padding-bottom: env(safe-area-inset-bottom)` overrides the shorthand but resolves to 0 (no viewport-fit=cover anywhere in `app/`; emitted meta verified `width=device-width, initial-scale=1`), so the cost line sits flush to the viewport edge on every platform. Evidence: journal shell-css finding. Areas: shell-css.
- **m-56 minor [OPEN]** `apps/web/app/dashboard/layout.module.css:100` — At the 900px collapse the rail keeps `flex:none` (~450px content height) while `.content` (`flex:1, min-height:0`) absorbs the deficit inside a fixed `100vh/100dvh overflow:hidden` shell, so on short landscape viewports the thread/composer clip to 0px. Evidence: journal shell-css finding. Areas: shell-css.

### Nits (19)

- **n-1 nit [OPEN]** `apps/web/app/gallery/page.tsx:389` — Fork-success links and failure alerts reuse line-clamped `.cardDetail` (2 lines, overflow hidden): fits today, but longer server text or larger fonts silently clip actionable content. Areas: gallery.
- **n-2 nit [OPEN]** `apps/web/app/gallery/page.tsx:312` — "Clear search" also resets the category pill (`clearFilters` clears both); tests acknowledge it, the label does not. Areas: gallery.
- **n-3 nit [OPEN]** `apps/web/app/dashboard/bots/[id]/page.tsx:1130` — Unconditional `attachment(s)` renders "1 attachment(s)" for a single file. Areas: dashboard-home-bots.
- **n-4 nit [OPEN]** `apps/web/app/dashboard/new/page.tsx:295` — `runId` interpolated into href without encodeURIComponent while the `[id]` page encodes the same link; safe only while runIds stay server UUIDs. Areas: dashboard-home-bots.
- **n-5 nit [OPEN]** `apps/web/app/api/chat/route.ts:130` — Comment claims to "Mirror readSpentCredits" in builder-runs.ts but the missing-row branches diverge (route returns NaN, builder returns 0); the route's direction is safer, the claim will mislead future sync edits. Areas: chat-api.
- **n-6 nit [OPEN]** `apps/web/app/api/chat/route.ts:343` (merged db-migrations + chat-api) — Budget refusal code `trial_budget_exceeded` is tier-blind for paid tiers exhausting their allowance, while the adjacent `message` is deliberately tier-aware; the code is also what the (now-fixed) client rendered. Duplicate merged. Areas: db-migrations, chat-api.
- **n-7 nit [OPEN]** `apps/web/app/api/webhooks/creem/route.ts:472` — One-time checkout grants are ledgered under recurring-grant reason `monthly_grant`; the true credit key rides only in meta, so reasons cannot distinguish recurring from one-time grants. Areas: db-migrations.
- **n-8 nit [OPEN]** `apps/web/app/page.tsx:46` — Trial-expiry copy inconsistent: FAQ "nothing is deleted" (unqualified, forever) vs bento "Planned: 12 months of data kept." Areas: landing.
- **n-9 nit [OPEN]** `apps/web/app/page.tsx:18` — Font loader fetches six Jakarta weights (300-800) while CSS uses only 400/500/600/700. Areas: landing.
- **n-10 nit [OPEN]** `apps/web/app/page.tsx:499` — Templates-section CTA "Templates preview — start free while in preview" navigates to #pricing, not templates. Areas: landing.
- **n-11 nit [OPEN]** `apps/web/app/dashboard/new/page.tsx:283` — "Say yes when the plan looks right" hint stays rendered after the build starts, contradicting the "Build started" status directly below. Areas: new-chat.
- **n-12 nit [OPEN]** `apps/web/app/dashboard/new/page.module.css:24` — `.newTop` back-bar lacks the position/z-index its siblings carry, so the ambient glow (z-index 0) paints over the link; clicks unaffected (pointer-events:none). Areas: shell-css.
- **n-13 nit [OPEN]** `apps/web/app/api/webhooks/creem/route.test.ts:1122` — Loud-skip justification rests on a false premise: no vitest config sets `silent`. Areas: ci-tests.
- **n-14 nit [OPEN]** `apps/web/eslint.config.mjs:1` — Nested web eslint config + `lint` script are dead under the root flat-config gate (`eslint .` never merges nested configs; CI never invokes the web script). Areas: ci-tests.
- **n-15 nit [OPEN]** `.github/workflows/deploy.yml:39` — Gate sequence lives in three hand-synced copies (root `ci` script, ci.yml, deploy.yml); drift is caught only by a human reading a comment. Areas: ci-tests.
- **n-16 nit [OPEN]** `apps/web/app/dashboard/bots/[id]/page.tsx:628` — Pre-flight scan hardcodes `capabilities: ['welcome']` though the route accepts six; moderation/XP bots get the wrong capability set scanned silently. Areas: dashboard-home-bots.
- **n-17 nit [OPEN]** `packages/ai/src/builder-prompt.ts:13` — `BUILDER_CALL_PARAMS.reasoningEffort: 'low'` is a dead field contradicting the lanes table's probed 400 on the builder lane's first route (lanes.ts:55-59); wiring it in future would reintroduce the 400 on every attempt. Areas: ai-package.
- **n-18 nit [OPEN]** `apps/web/app/api/auth/login/route.ts:15` — Login catch swallows all failures without logging, hiding OAuth misconfiguration from server logs (chat/verdict spend paths log their swallowed causes). Areas: auth-trial.
- **n-19 nit [OPEN]** `apps/web/lib/auth/session.ts:294` — `returnTo` is plumbed (mint arg, `return_to` column at :361) but never minted with a value nor honored after callback. Areas: auth-trial.

## Ranked defects — FIXED, verified on disk (13)

- **F-1 major [FIXED]** Verdict turn-cap ask-line kill (builder-api `verdict/route.ts:63`, TURN_MAX 500 + head slice). Fixed: `TURN_MAX = 2000` in `apps/web/lib/verdict/bounds.ts:24` (equals chat MESSAGE_MAX, documented why); both sides send/receive through kept-ends `boundedView` (route :424/:439-440, new page :126); reply tail likewise kept (`REPLY_TAIL_MAX`, route :440). Shared-bounds extraction done (bounds.ts header documents the history).
- **F-2 major [FIXED]** Verdict-route missing budget gate (merged ai-package + db-migrations + builder-api + new-chat). Fixed: pre-model `checkBudget` gate at `apps/web/app/api/builder/verdict/route.ts:385-414` — same estimate shape, same refusal code/sentence, no model call and no spend row on refusal, 500 on unreadable meter; call site pre-gates tier via `isPlanTier` (:394) so the prototype-key path (m-18) cannot be reached here.
- **F-3 major [FIXED]** RUNBOOK glob-loop re-arming trial clocks (db-migrations `RUNBOOK.md:157`). Fixed: `infra/RUNBOOK.md:154-165` now mandates one-file-per-command forward-only application, names 0010 as never-re-run, and the glob loop is gone; `Docs/10_deployment.md:21` locks "never a glob loop" with the same rationale.
- **F-4 major [FIXED]** Rollback never re-syncing runtime rows (db-migrations `rollback/route.ts:212`). Fixed: `apps/web/app/api/spec/rollback/route.ts:292-297` translates the target spec and syncs `bot_runtime_config` inside the same transaction (pointer move + audit row roll back together on failure).
- **F-5 major [FIXED]** Gallery fork-403 raw code (gallery `gallery/page.tsx:71`). Fixed: page imports shared `forkErrorMessage` (`gallery/page.tsx:10`, used :196); `apps/web/lib/http/refusal.ts:52-54` prefers `message`, degrades to a status sentence — the locked trial sentences now render instead of `trial_bot_limit`.
- **F-6 blocker [FIXED]** Draft-wipe on save (bot-detail `[id]/page.tsx:717`). Fixed: lines 752-791 — base comes from what the read RETURNED (not stale closure state); `unreadable` refuses the save with "nothing was saved," and only definitive `absent` proceeds without a base.
- **F-7 major [FIXED]** Dead "Continue interview" button (merged bot-detail `:934` + dashboard-home-bots). Fixed: `apps/web/app/dashboard/bots/[id]/page.tsx:1024-1032` — honestly disabled with `aria-disabled` + "Coming soon" title, matching the Upgrade control, with a comment stating the interview page cannot resume an existing bot.
- **F-8 major [FIXED]** Start-build swallowing 403 trial message (bot-detail `[id]/page.tsx:810`). Fixed: lines 887-894 — `readRefusalMessage(payload, { allowErrorFallback: false })` surfaces the server's locked sentence byte-for-byte; generic line kept only for unknown errors.
- **F-9 major [FIXED]** Chat hook dropping refusal sentence (chat-api `thread.ts:134`). Fixed: `apps/web/lib/chat/thread.ts:135-144` — `readHttpError` prefers `message`, falls back to `error`; trial clock/budget refusals now render the KI-033 sentence instead of the code.
- **F-10 major [FIXED]** Gateway relogin latch (gateway `start.ts:158`). Fixed: latch cleared on attach (`start.ts:130-134`) and on destroy (:238-241); factory re-arms the ClientReady listener per fresh client with an identity guard (:201-215), per the lifecycle-ownership comment (:180-187).
- **F-11 major [FIXED]** Orphaned module polls across relogin/removeBot (gateway `start.ts:97`). Fixed: `stopModulesFor` consumed at the single destroy seam (:224-238) and on attach (:139); the former NOTE is resolved in code comments.
- **F-12 major [FIXED]** Giveaway 100-entrant cap (gateway `giveaway.ts:161`). Fixed: `fetchEntrantIds` pages with `{ limit: REACTION_PAGE_SIZE, after }` cursors (`apps/gateway/src/runtime/games/giveaway.ts:21-29, 185-212`), documented against the discord.js default.
- **F-13 (supplementary) [FIXED]** Refusal-reader unification (enabler for F-5/F-8/F-9): `apps/web/lib/http/refusal.ts:1-54` is now the single message-preferred source with the bot-detail `allowErrorFallback: false` difference preserved by option; interview/gallery/bot-detail callers import from it.

## Dropped (0)
No finding was dropped. Every candidate held up against source on re-verification, including the near-misses considered for dropping: gallery clamp (n-1) and Clear-search label (n-2) are genuine (if small) copy/clipping warts; the `runId` encoding nit (n-4) is a live inconsistency even if currently safe; the tier-blind code (n-6) states something false about paid accounts in a machine channel; the "Mirrors" comment (n-5) is factually wrong and will mislead sync edits. Silent truncation was not used: all 114 raw findings map to the 102 unique IDs above via the 10 merges listed.

## Notes (cross-area patterns)
- **Refusal-reader class (fixed):** four surfaces read only `error` and dropped the locked KI-033 sentence — gallery fork (F-5), bot-detail start-build (F-8), chat hook (F-9), plus the shared-unification enabler (F-13). The remaining tail is m-1: code-only shapes (401/422) still surface raw codes because no `message` exists to prefer. Closing m-1 means giving those routes sentences, not touching the readers.
- **Kept-ends class (fixed):** the verdict ask-line kill (F-1) came from head-slicing a turn whose meaning lives at its END; the fix (shared `boundedView`) is the pattern to reuse anywhere else long text is capped before a suffix check. m-39 is the same class in miniature (clamp vs guard ordering).
- **Dead-UI-without-honesty-marker pattern:** "Continue interview" is now the exemplar fix (F-7: disabled + Coming soon). The same violation remains OPEN at home preset cards (m-8) and the image-attach affordance (M-6, worse: it destroys user input silently).
- **Trial-signal propagation gaps:** unauthorized (m-2), trial banner (m-3), and loading flashes (m-4/m-5) are one story — list surfaces that do not know the session state the detail page already handles. Recommend one fix task owning all list-surface session signals.
- **Money-gate gaps (after F-2):** the verdict hole is closed, but chat still undercounts on abort (M-11), admits concurrent overruns (m-23), and under-reserves failover turns (m-24); the worker still spends one call before confirming the bot (m-28). None is individually large; together they bound how literally "never cross the grant" holds.
- **Test-harness classes (ci-tests):** red-on-master (M-12) and no-runner/no-journal (M-13) are the two majors; under them sit four minors of the same family — environment-dependent suites (m-50), skip-without-return (m-51), dead migration-first paths (m-52), and gates that silently skip or abort early (m-53/m-54). Fixing M-13 (a real runner + journal) plausibly dissolves m-50 and m-52 as side effects; sequence accordingly.
- **Migration discipline:** F-3/F-4 close the two sharp edges (re-run semantics, rollback parity). The standing rule both fixes encode — forward-only, one file per command, pointer moves ship with their row syncs — is worth stating once in the contributing path so the next migration inherits it.
- **No severity overrides were needed:** the three cross-area severity splits (CTA major/minor → M-3 major; OAuth-param major/minor → M-5 major; attachments minor/major/major → M-6 major; length-guard minor/nit → m-39 minor) were all resolved within the cited range toward the broken-flow end, and are recorded above rather than silently picked.
