# Task Report: fix-newpage-523-1800

## Status

SUCCESS

Option **A** realized — with two premises of the assignment falsified by measurement and the
substituted evidence disclosed in full below (Open Questions 1–3). The vacuous thread-wide negative is
gone; a load-bearing scoped negative now sits on a render that genuinely carries a thinking row. Suite
stays green at **49 passed (49)**, all gates exit 0, and the new assertion is proven to fire
(mutation B1, red at the new assertion, exit 1) and to be the **sole** catcher in this file for the
shape it guards (B1-isolation, green without it).

---

## Files Touched

- **MODIFIED**: `apps/web/app/dashboard/new/page.test.tsx`
  - SHA256 **before**: `7FF29EB258A45C6E5FD5B0EAA6D4104AA660C323422FD3F554FC034C6A7F9294` (1988 lines)
  - SHA256 **after**:  `4570CDF5587EBBD3FB218B1874774F2CD23C9C749B41156361A0979D62D41FC5` (2016 lines)
- **CREATED**: `Agent Reports/2026-09-24-1800_fix523_FIX_newpage-523neg.md` (this report)
- **DELETED**: none in-repo. (`%TEMP%\f523-instrument` — the out-of-repo mutation instrument — was
  removed under the mandated reparse-point guard; see Verification.)

Read-only, byte-identical before and after (SHA256 unchanged, confirmed on the final tree):

| File | SHA256 (unchanged) |
|---|---|
| `apps/web/components/ui/thinking-trace.tsx` | `5D60C4B46F184A20C3AD12A9A40AD46A6D8872B96312D0BD47CFF20C71DFC46D` |
| `apps/web/components/ui/chat-thread.tsx` | `1129CF7250B8066258F31C47C095729231B0252F481B04AB1263DCB268BAAF62` |
| `apps/web/app/dashboard/new/page.tsx` | `39233C7F0357451E34FCFFFDC5A66BCB4CACBA1B84E3D1CE6F63E9B58BEAA7D8` |
| `apps/web/components/ui/use-chat-stream.ts` | `8B0B38C58A544EB1D826184A304478B4FA9EECB785C9A947CD43A42FF47FE733` |

### The exact change, with final line numbers

| Line | Content | Note |
|---|---|---|
| `:522` | `expect(thread.textContent).toContain('Düşündü');` | **untouched** — direct pin, exactly 1 occurrence |
| `:523` | `expectNoEnglishResidue(PARKED_RESIDUE);` | moved up one line (was `:524`); meaning unchanged |
| `:529` | `const live = sseStream();` | second turn's own stream |
| `:530–537` | `vi.stubGlobal('fetch', …)` | re-stub; `/api/bots` delegated to the **original** `fetchStub(url)`, so the mint-once assertion at `:556` still watches the whole test, not only its first half |
| `:538` | `await submitCreation('Second question');` | the second, genuinely live turn |
| `:539–541` | `await waitFor(… getAllByRole('button', { name: 'Düşünüyor' }) …)` | live-row precondition |
| `:547` | `await flushSettled();` | **durable** — lets the second turn's own chain settle |
| `:548` | `expect(screen.getAllByRole('button', { name: 'Düşünüyor' }).length).toBeGreaterThan(0);` | synchronous re-assert: the row is still live after settling |
| `:549–550` | `const parkedRow = …closest('li'); expect(parkedRow).not.toBeNull();` | the parked row, narrowed to its `<li>` |
| `:551` | `expect(parkedRow?.textContent).toContain('Düşündü');` | scoped positive |
| **`:552`** | **`expect(parkedRow?.textContent).not.toContain('Düşünüyor');`** | **the new load-bearing negative** |
| — | old `:523` `expect(thread.textContent).not.toContain('Düşünüyor');` | **removed** — 0 occurrences remain |

Preserved untouched, verified on the final bytes: `:522` direct pin (1) · `PARKED_RESIDUE` sweep (1) ·
`THINKING_RESIDUE` sweep (1) · `:494` thinking pin (1) · helper `expectNoEnglishResidue` `:390–395`
(1) · `ENGLISH_RESIDUE` **16 entries**, still free of `Thinking`/`Thought`/`took` · no
`.only`/`.skip`/`.todo` (grep exit **1**) · 43 `it(` + 2 `it.each(` = the 49 tests. No `it` was added
or removed; the pinned count stays **49**.

---

## Dependencies Added

None. No install command was run; no manifest was touched.

---

## Assumptions Made

1. **No web research was needed, and none was done.** Nothing in this task depends on a fact that can
   change: the inputs are bytes on disk (an assertion in one test file) and a local Vitest run. No
   library version, model name, API surface, or price is in play. Per the task's own RESEARCH FIRST
   directive, no live source is cited because no live fact informed the change.
2. **No OSS pattern lookup was needed.** This is a test-internal guard fix — one assertion in one test
   file, with no library or architectural pattern to adopt. Stated in one line as instructed.
3. **The negative is scoped to the parked row, not thread-wide.** On a genuine two-turn render the
   thread really does contain a live `Düşünüyor`; a thread-wide negative there is objectively false
   (measured: probe P2d went red at the scoped-region line with the thread-wide sense). The claim the
   assertion is entitled to make is the exclusivity one: *this* row is parked, so *this* row must not
   carry the live label — on a render that does hold one.
4. **The second-turn stub is widened, so the second turn is genuinely live.** The reviewer's Open Q1
   recipe, taken literally, does not produce one (Open Question 1).
5. **`flushSettled()` + synchronous re-assert is a real hardening, not cosmetics.** Discovery F1
   (Open Question 4) showed the `waitFor` could be satisfied by a transient render, which would have
   let the new negative silently degrade back into the vacuous shape it replaces.
6. **`apps/web/node_modules/next/dist/docs` is ABSENT** — recorded, not silently skipped. It is absent
   on this tree, and this task needs no Next API (a test-internal assertion edit reads no framework
   surface). The `AGENTS.md` note about the re-added block is likewise not applicable: that block was
   not touched.
7. **`took` remains a substring sweep** (it is the `sweep` entry, not a direct pin). Disclosed as a
   limitation rather than silently treated as equal to a pin.

---

## Open Questions for Orchestrator

**The two premises of this assignment did not survive measurement. Both are reported with the
measurement, not with an argument.**

1. **The reviewer's Open Q1 recipe is mechanically impossible inside that `it`.** The `it`'s stub hands
   out **one already-read stream** for every non-mint URL, so a second `submitCreation` lands on the
   **error** branch. Probe **P2b** (recipe verbatim, with its own precondition asserted) failed:
   `AssertionError: A-LITERAL precondition: a thinking row is mounted on this render: expected null
   not to be null`. There is no second live turn to assert against until the stub is widened — which is
   what the delivered change does (delegating `/api/bots` to the original stub so nothing is hidden
   from the mint-once count).
2. **The prescribed option-A mutation direction is logically impossible for any
   `not.toContain('Düşünüyor')`.** Reverting the thinking label to English *removes* `Düşünüyor` from
   the live row, so the negative becomes **trivially true** — it can only get greener, never redder.
   Measured: **A1** red at `:494:32` (the pre-existing pin, i.e. not at the new assertion); **A1b**
   (that pin and the `THINKING_RESIDUE` sweep neutralized) red at the live-row precondition — the new
   negative still never fires. The mutation that actually makes the new assertion fail must *add* a
   live-thinking render into the parked row; that is **B1**, and it fails **at the new assertion**
   (`:552:40`, exit 1). B1 is therefore submitted as strictly stronger evidence than the prescribed
   direction, and it is the direction the defect class actually takes.
3. **`:523` was NOT vacuous by construction — it was the sole catcher in this file, and its removal is
   compensated rather than absorbed.** Probe **P2a** (the parked/done branch also mounts a live trace —
   the exclusivity defect) went red at exactly `:523:36`; **P2e** (same shape, `:523` deleted) read
   **49 passed, exit 0**, with sibling suites still green. So the old line was true on the single-turn
   render *situationally*, not by construction. Deleting it silently would have removed this file's only
   guard for that shape. The delivered scoped negative restores it on a render where it can fire, and
   **B1-isolation** (scoped negative deleted, same defect injected) reads **49 passed** — confirming the
   new line is again the **sole** catcher, so no catching power was lost.
4. **The `waitFor` precondition was satisfiable by a transient render — hardened.** Probe **F1**
   initially came back **green 49/49**, which falsified the expectation. A dump probe showed
   `chatPosts=1 thinkingBtns=0`: the second submit never happened, and the green run proved the
   `waitFor` had been satisfied by a transient live-thinking render. Fixed **in the repo, not only in
   the probe** by adding `await flushSettled()` + a synchronous re-assert (`:547–548`). F1 now correctly
   goes **red**, exit 1, at the precondition. Worth knowing for the sibling wave: any `waitFor` used as
   a *precondition for a negative assertion* is unsound without a settle-and-re-assert.
5. **A correction for the record, with no action requested:** the delivered evidence for option A is
   B1, not the prescribed label revert. If the orchestrator wants the literal
   "label → English" mutation to be the recorded proof, it cannot be — for the reason in Open Question 2
   — and the honest red for that direction is A1/A1b (caught by the pre-existing guards, not by the new
   line).
6. **Two gate defects I introduced were caught and fixed before this report** — disclosed rather than
   quietly corrected: `tsc --noEmit` initially failed with `page.test.tsx(536,28): error TS2554: Expected
   1 arguments, but got 2` (the `it`'s own stub is declared `(url: string)` — one parameter — so my
   delegate call `fetchStub(url, init)` passed one argument too many), and `prettier --check` initially
   failed on the same ternary's line wrapping. Both are fixed; the final bytes carry SHA256
   `4570CDF5…` and all gates are exit 0. **The full mutation table was re-measured on the corrected
   bytes** — the earlier table is superseded, and the numbers below are the final ones.
7. **`%TEMP%\f7t3_*` was left intact** as instructed (verified present before and after cleanup).
8. **`took` is still a substring sweep.** Probe **P2F** (a new static Turkish string added to the page)
   passes with the shipped form — but the **old** thread-wide form did not catch it either (measured),
   so nothing was given up and nothing was gained. A new English *string* on this page is caught by the
   page-level `ENGLISH_RESIDUE`; a new **Turkish** one is outside every list's purpose.

---

## Public Interface Exposed

**Test-internal only. No production surface changed.** The `it` `submitting streams the reply into a
thread with botId null` now performs a second `submitCreation('Second question')` and re-stubs global
`fetch` for that half. No exported symbol, component prop, API endpoint, or signature changed; no file
outside the test file was modified.

---

## Verification

### Gates on the real tree (true exit codes, never through a pipe)

| Gate | Command | Result |
|---|---|---|
| Focused suite | `node ./node_modules/vitest/vitest.mjs run app/dashboard/new/page.test.tsx` (cwd `apps/web`) | banner `RUN v5.0.0`, `Test Files 1 passed (1)`, `Tests 49 passed (49)`, **EXIT=0** |
| Typecheck | `npx tsc --noEmit` | **EXIT=0** |
| tsc instrument validated | `tsc --noEmit --listFiles` | the test file is **in the program** (1 of 847 entries) — the green is not a "file never inspected" green |
| Lint | `npx eslint --max-warnings 0 app/dashboard/new/page.test.tsx` | **EXIT=0** |
| eslint instrument validated | `--format json` | 1 result, correct `filePath`, **0 messages, 0 suppressed** |
| Format | `npx prettier --check app/dashboard/new/page.test.tsx` | `All matched files use Prettier code style!` **EXIT=0** |
| Forbidden markers | `grep -nE "\.(only\|skip\|todo)\("` | no matches, **exit 1** |

### Mutation table — measured on the FINAL bytes (`4570CDF5…`), out-of-repo copy only

Baseline re-copied per row; parity between repo and copy asserted byte-for-byte for all five in-scope
files before use; the copy's reparse-point count asserted **0** before use and before the delete; the
pinned runner used throughout; exits are true exit codes.

| # | Probe | Mutation | Result | Exit |
|---|---|---|---|---|
| 1 | **CONTROL** | none (unmodified copy) | **49 passed (49)** | **0** |
| 2 | A1 | thinking label → English | red — `expected 'A welcome bot for my study serverThin…' to contain 'Düşünüyor'` at `494:32` | 1 |
| 3 | A1b | as A1 + `:494` pin and `THINKING_RESIDUE` sweep neutralized | red at the live-row **precondition** — the new negative never fires | 1 |
| 4 | **B1** | **parked row also mounts a live trace** (nothing neutralized) | **red — `expected 'Got it — drafting.Düşünüyor · 0sWeigh…' not to contain 'Düşünüyor'` at `552:40`** | **1** |
| 5 | B1-isolation | as B1 + the scoped negative deleted | **49 passed (49)** — the scoped negative is the **sole** catcher | 0 |
| 6 | F1 | second turn forced onto the error branch | red at the durable precondition `550:19` (mutated numbering) | 1 |
| 7 | C1 | done-branch label → English | red — `expected '…' to contain 'Düşündü'` at `522:32` | 1 |
| 8 | C1b | as C1 + `:522` pin and scoped positive neutralized | red — `thought label still English: Thought` (`393:59` from `522:5`) | 1 |
| 9 | C2 | elapsed suffix → English | red — `elapsed suffix still English: took` (`393:59` from `523:5`) | 1 |
| 10 | C2b | as C2 + `PARKED_RESIDUE` sweep neutralized | **49 passed (49)** — the sweep was the only catcher | 0 |
| 11 | C3 | thinking label → English + `:494` pin neutralized | red — `thinking label still English: Thinking` (`393:59` from `496:5`) | 1 |
| 12 | E2 | full English, shipped guards intact | red at `494:32` | 1 |
| 13 | E1 | old thread-wide form reconstructed + English + thinking guards down | red — old form cannot fire; caught by the precondition | 1 |
| 14 | D1 | dead-entry design (row words folded into the page-level empty-page list, all row guards dropped) + full English | red at `538:11` — the F12B/F12C class is still caught, not reintroduced | 1 |
| 15 | P2F | new static **Turkish** string on the page | 49 passed (49) — no coverage given up; the old form did not catch it either | 0 |

**Option B's required evidence is satisfied too, for the record:** rows 7–11 show the remaining guards
still catch English on both branches (`:522` direct pin; the `PARKED_RESIDUE` sweep with its
`thought label`/`elapsed suffix` entries), and rows 3, 5, 13 show no guard depends on the removed line.
Option A was taken, so B was not needed — but the probe set proves the same conclusion.

### Cleanup (mandated guard)

- The guard's reparse detector was **validated before use**: a fixture junction was created and the
  detector reported it (`FOUND: …\holder\link` → `GUARD DETECTOR VALIDATED`).
- Precondition scan of `%TEMP%\f523-instrument`: **0 reparse points** → `PRECONDITION OK: safe to
  delete`. Guarded delete ran; `still exists after delete: false`.
- `%TEMP%\f523-instrument` verified **gone**; `%TEMP%\f7t3_*` verified **present and untouched** (7
  entries) after cleanup; my own scratch fixtures removed likewise (junction unlinked with `rmdir`
  first — the target survived, confirming no traversal).

---

## Known Limitations

- **No browser drive.** jsdom + Testing Library only; the exercised path is the suite. The flow
  completed under test, not in a real browser.
- **Verified against an actively-written tree.** HEAD is `d9cf8d7` and the whole tree is uncommitted
  with other agents' work in flight; other files may change after this report. Nothing was committed.
- **The out-of-repo copy did not materialize `@corvus/web`.** Only this one spec was run in the copy
  (cross-package suites were not re-run there); the focused suite on the **real** tree is the shipped
  evidence.
- **`took` is a substring sweep**, and `Düşünüyor` is asserted by role-name lookups rather than by raw
  substring in the new negative — a new Turkish string elsewhere on the page is outside every list's
  purpose (P2F).
- **The durable-precondition hardening is exercised by mutation F1, not by a dedicated `it`.** No new
  test was added, per the instruction to prefer not changing the pinned count.
- The two gate defects noted in Open Question 6 were found by the gates themselves, not by the
  mutation table; the table was re-measured after the fix, but a re-review of the final bytes is the
  reviewer's call, not mine.
