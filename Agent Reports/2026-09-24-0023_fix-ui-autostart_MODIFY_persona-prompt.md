# Task Report: fix-ui-autostart

Timestamp: 2026-09-24-0023 (system clock, TST)

Report filename note: the task text named `..._MODIFY_dashboard-new.md`, but the
`<YYYY-MM-DD-HHMM>_<agent-id>_<task-type>_<component>` schema applies and I touched
`persona-prompt`, not `dashboard-new` (which the spec explicitly forbade). Filename
follows the schema with the real clock date, not the task text's stale date.

## Status
SUCCESS (copy defect fixed at source, proven on the live page) — but the founder's
end-to-end flow is **still blocked by two defects outside this task's file scope**.
Both are escalated below with live evidence. The copy fix was necessary and is
verified; it is not sufficient on its own.

## Files Touched
- MODIFIED: `packages/ai/src/persona-prompt.ts` — replaced the stale control-naming
  lines (:15-16) and extended the ask-line rule into a re-ask rule (:23); rebuilt
  `dist/` (gitignored, regenerated by `npm run build`).
- MODIFIED: `packages/ai/src/persona-prompt.test.ts` — the two stale "names the
  Build this bot button as the only runner" pins (:103-113) became no-control pins
  (the harness guard); the honesty pin at :175 became a no-control/wording pin; the
  re-ask rule and its byte-exact duplicate ask line gained pins.
- NOT touched (per spec, founder-locked): `apps/web/app/dashboard/new/page.tsx`,
  `page.test.tsx`, `apps/web/app/api/builder/verdict/route.ts`. Nothing else.

## Dependencies Added
- none (declare-only: none needed)

## What changed, exactly

`packages/ai/src/persona-prompt.ts:15-16`, before → after:

```
- Describe what will happen, offer next steps, and point to the product buttons that run draft, simulate and publish.
- Drafts, simulations and publishes run only through the product buttons such as Build this bot. Chat text describes, execution stays out of band.
+ Describe what will happen, offer next steps in words, and never name a control or ask the owner to press or click anything.
+ Drafts, simulations and publishes are started by the product from the page, never by chat text: never ask the owner to run them another way. Chat text describes, execution stays out of band.
```

New line after the C1 ask rule (now prompt lines 22-23, both carrying the ask line byte-exact):

```
After at most 2-3 short questions, post a 2-4 bullet plan summary ending with this exact line: Can I start? Reply yes to build.
When the owner signals acceptance in any wording (yes, evet, tamam, tamamdir, basla, "sen karar ver", "you decide"), you must post the 2-4 bullet plan AGAIN, ending with that same exact line: Can I start? Reply yes to build. The product starts the build from the page only when the owner's next message follows a turn that ends with that line, so it must appear in the reply to an acceptance signal itself, not only in the first plan.
```

Invariants held: 16 lines plain / 17 with botName (cap 20); no `!`; no emoji;
`Can I start?` byte-identical in both copies; every honesty line intact
("never claim a build started, is running, or is done", "You create nothing in chat").

## Does the fix work? (verified on the live path, not just green gates)

The dev stack was already serving `127.0.0.1:3000`. I drove the founder's exact
flow in a real browser (Playwright, dev session cookie): typed
`Bana Mee6 benzeri bir discord botu kur`, then `tamamdir basla`.

**Turn 1 reply** (live model output, Turkish, ends byte-exactly):

```
Hangi özellikleri istediğini yaz, ya da hepsini istiyorsan söyle.

Can I start? Reply yes to build.
```

**Turn 2 reply to the acceptance signal** (`tamamdir basla`) — the new re-ask rule
fired; the model re-posted the plan and ended with the ask line again:

```
- Seviye sistemi: mesaj yazan üyeler XP kazanır, belirli seviyelerde otomatik rol verilir
- Moderasyon: ban, kick, mute ve uyarı komutları, kayıt tutulur
- Hoş geldin mesajı ve otorol: yeni üyeye otomatik rol verilir ve kanala mesaj gönderilir
- Müzik: sesli kanalda şarkı çalma, duraklatma, atlama

Can I start? Reply yes to build.
```

Checks on both rendered turns: `mentions Build this bot: false`; `mentions a
button: false`; `ends with ask line: true`. **The founder's literal complaint
("hala build this bot diyo") is fixed on the live page.**

## Verification (gates, on the merged tree)

| gate | command | result |
|---|---|---|
| ai unit | `npx vitest run` (packages/ai) | 157 passed / 6 files (41 in persona-prompt.test.ts) |
| ai typecheck | `npx tsc --noEmit` | clean |
| ai build | `npm run build` | clean; `dist/persona-prompt.js` carries the new copy, 0 hits for `Build this bot` |
| web consumers | `npx vitest run app/api/chat/route.test.ts app/api/builder/verdict/route.test.ts app/dashboard/new/page.test.tsx` | 106 passed / 3 files, **zero edits to those files** (`chat/route.test.ts` derives expectations from `buildPersonaPrompt()`, so it tracks the prompt by construction) |
| web typecheck | `npx tsc --noEmit` (apps/web) | clean |
| repo lint | `npx eslint . --max-warnings 0` | clean |
| format | `npx prettier --check` (both changed files) | clean |
| full workspace | `npm test` | ai 157 ✓, spec 56 ✓, web 927 ✓, testbot 103 ✓, gateway 462/464 — see note |

Gateway note: `apps/gateway/src/launch-blockers.test.ts > C — 10k-XP storm then
clean shutdown flushes everything` failed once in the parallel full run and
**passes in isolation** (`4 passed`, 9.99s). It is a real-Postgres timing test with
zero references to the persona prompt — pre-existing flake, unrelated to this change.

**Guard proven by breaking it (not just by passing):** I injected the stale copy
back (`point to the product buttons…`) and the guard failed exactly 3 tests, then
restored and re-verified 41/41 green.

## Assumptions Made
- The re-ask rule must **quote the ask line byte-exact a second time** rather than
  refer to it ("that same line"), because the model cannot copy a string it was
  never given at the point of the rule, and the pin must be able to assert two real
  occurrences. The pinned test on the count is what caught my first attempt that
  only referenced the line — it failed, and I changed the prompt, not the test.
- The new no-control rule forbids naming *any* control, not just `Build this bot`,
  because the same prompt serves the button-less `/dashboard/new` and the bot detail
  page; a future rename/removal of a detail-page button must not re-open this class.
- The Turkish acceptance variants named in the prompt are the ones the founder
  actually typed in this thread (plus `evet`), not an exhaustive list — the rule
  opens with "any wording" so naming them is a hint, not a closed set.
- `packages/ai/dist/` is gitignored and is what `apps/web` resolves at runtime, so
  I ran `npm run build` (the repo's own script) to refresh it. No manifest edited,
  no install run.

## Open Questions for Orchestrator — TWO BLOCKERS OUTSIDE MY FILE SCOPE

**No source file was edited for these; SCOPE GUARD raised instead of expanding.**
Both reproduce on the founder's exact path and either one alone still leaves the
thread dead. Verified live at 2026-09-24-0020 against the running dev stack.

**B1 — `POST /api/bots` refuses the mint: `403 trial_bot_limit`.**

```
POST /api/bots {"botName":"Probe bot"} -> 403
{"error":"trial_bot_limit","message":"Free 3-day trial — 1 bot, 100 AI credits."}
```

The account already holds one bot, so `mintOnce` (`page.tsx:276-318`) fails on its
very first call. With `setBotId` never committing, the verdict effect's
`if (botId === null) return;` (`page.tsx:208`) can never pass — so **no verdict POST
is possible for any account at the trial bot limit, regardless of what the assistant
replies.** This is the KI-033 trial gate (`mintGate`, `apps/web/app/api/bots/route.ts:138`)
working as designed; the question is product copy, not code.

Confirmed in the browser on `/dashboard/new` after the acceptance reply: the mint
error **does** surface, as `<p role="alert">` at `page.tsx:398` reading
`Free 3-day trial — 1 bot, 100 AI credits.` (that string is `TRIAL_BOT_LIMIT_MESSAGE`,
identical to `TRIAL_DEAL`, `apps/web/lib/bots.ts:50,57`). The copy gap is that the
page **also** renders `Saving your bot…` at the same time: `showSaving`
(`page.tsx:335-351`) is derived from ask-line adjacency and **ignores `mintError`**, so
the user sees a permanent "Saving…" (implying progress is under way) next to a 403
refusal, and the Saving line never clears. Needs a product decision (raise the trial
bot limit? route existing-bot owners to the detail page?) plus a small one-line
`showSaving` guard (`mintError === null &&`) — both outside my scope.

**B2 — `POST /api/builder/verdict` returns `500 {"error":"could not judge reply"}`.**

```
POST /api/builder/verdict (real botId, plan ending "Can I start?", user "tamamdir basla")
-> 500 in 0.25s   (dev log: "POST /api/builder/verdict 500 in 250ms")
```

Isolated to the route, not the key or the provider: the **same persona lane and key
answer normally** — `POST /api/chat` with `history:[]` returned `200` and a full
Turkish reply in 12s. The route calls the provider with a **system-only** message
array (`route.ts:434-445` and `:473-476`), while the working worker path sends
`[system, user]`. This matches the required-context report
(`Agent Reports/2026-09-23_local-verify_dashboard-new.md:35`), which reproduced the
same 500 independently and dated it to that route's call sites. That report is my
whitelisted context; I did not re-derive it from the provider, but I did confirm the
symptom live and the surrounding facts.

Neither B1 nor B2 is reachable by editing `persona-prompt.ts` / its test — my two
files are fixed and green. **Recommendation (for the orchestrator, not a unilateral
move):** the copy fix lands as-is (it removes the phantom button and re-arms the ask
line, both proven live), then a follow-up task fixes B2's two call sites and a second
task takes the B1 product decision plus the `showSaving`/`mintError` copy gap.

## Public Interface Exposed
- `buildPersonaPrompt(opts?: { botName?: string }): string` — **unchanged signature**.
  Output changes: lines 15-16 no longer name any control; a new re-ask rule line
  follows the C1 ask rule. The two ask-line copies are byte-identical to each other
  and to the substring both gates match (`page.tsx:52`, `route.ts:109`).
- No other exported symbol changed. `buildVerdictPrompt` / `buildBriefPrompt`
  untouched.

## Known Limitations
- I did not edit the page or route, so **the founder still cannot complete the flow**:
  with this account, the mint 403s before any verdict POST (B1), and a verdict POST
  would 500 anyway (B2). The copy layer is correct; the two blockers above are the
  remaining cause of "hala ... diyo" being unfixable from the prompt side alone.
- The browser pass used the dev account (one existing bot) — which is exactly why B1
  surfaced. On a fresh account with room to mint, this fix's ask-line path would reach
  the verdict route and hit B2 instead.
- No production/deployed environment was exercised; local dev stack only.
- The gateway flake was observed once and not chased further; it is unrelated to
  these files and passes in isolation.
