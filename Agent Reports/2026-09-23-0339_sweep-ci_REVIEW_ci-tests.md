# Sweep Inspection: ci-tests

Persisted 2026-09-23-0339 by orchestrator from inspector agent result (no synthesis yet — raw area output).

AREA: ci-tests
FINDINGS:
- file: apps/web/app/api/interview/interview.test.ts | line: 289 | severity: major | summary: CI gate is deterministically red on master — the KI-033 one-bot trial cap collides with the suite's single shared `owner` account, so every startBot after the first mint 403s | evidence: `beforeAll` mints one owner account (shared by all tests), then tests at :392/:403/:429/:444/:458 each call `startBot(...)` against it, while `mintGate` refuses the second mint (`if (liveBotCount >= 1)` in apps/web/lib/bots.ts:111). Confirmed by fixwave-batch reviewer §4: "FAIL interview.test.ts (3) + templates.test.ts (2)... deterministic 403". Docs/PLAN.md follow-up "CI interview/templates tohumlama" still open.
- file: infra/RUNBOOK.md | line: 155 | severity: major | summary: No migration runner and no applied-migrations journal anywhere — CI boots an empty DB and every suite hand-applies a different ad-hoc subset of apps/gateway/drizzle/*.sql with swallowed failures; a pristine DB measurably fails | evidence: RUNBOOK §7.2.5 hand-run procedure; no `drizzle/meta` journal, no migrate script in any workspace package.json. Reviewer proved consequence: pristine DB takes failures from 3 to 8 (`expected 500 to be 200` — tables don't exist; FALLBACK_DDL self-heal incomplete). Failure count is a function of database state.
- file: apps/web/app/api/webhooks/creem/route.test.ts | line: 1203 | severity: minor | summary: Money-guard suite order-dependent under full-suite parallel load: schema only from inline FALLBACK_DDL, no `accounts.tier` self-heal, so when another suite creates `accounts` via 0002_v11.sql (no tier), `column "tier" does not exist` reds the run | evidence: FALLBACK_DDL `CREATE TABLE IF NOT EXISTS` (:1147-1156) is a no-op when `accounts` already exists; only 0008 adds tier and no web suite applies 0008; siblings (activity/route.test.ts:132-134) carry the self-heal, creem does not. Passes 60/60 in isolation, fails in full-suite runs.
- file: apps/gateway/src/launch-blockers.test.ts | line: 303 | severity: minor | summary: All four real-PG launch-blocker tests call `ctx.skip()` with no `return`, so on a DB-less run execution continues into live queries and burns each 60–120s cap before "skipping" — up to ~6 wasted minutes per no-DB run | evidence: `if (PG_UNREACHABLE) { ctx.skip(); }` with no return at :303 (120s), :402-404 (B1, 60s), :446-448 (B2, 60s), :498-500 (C, 120s); pool has no `connectionTimeoutMillis` (:65). Siblings do `ctx.skip(); return;` (start.test.ts:737, tier-resolver.test.ts:139-140).
- file: apps/web/lib/auth/session-db.test.ts | line: 553 | severity: minor | summary: ~11 web suites' "try the real sibling migrations first" path is dead code — under vitest `new URL(relative, import.meta.url)` resolves to the Vite dev-server origin, readFile rejects, and the empty catch silently routes every suite onto fallback DDL, so migration↔fallback drift is never exercised | evidence: comment at :552-558; same swallowed pattern in publish.test.ts:136-143, interview.test.ts:121, spec.test.ts:90, simulate.test.ts:86, templates.test.ts:118, rollback.test.ts:140, bots/route.test.ts:104, activity/route.test.ts:121, builder/route.test.ts:361, builder/start/route.test.ts:587, preflight.test.ts:612.
- file: package.json | line: 18 | severity: minor | summary: Workspace test gate is latent silent-skip: `--if-present` silently passes over any workspace lacking a `test` script (LESSONS L-009 incident class) | evidence: `"test": "node -e ... || npm run test --workspaces --if-present"`; same shape on `typecheck` (:14) and `format` (:16).
- file: package.json | line: 18 | severity: minor | summary: `npm test --workspaces` runs workspaces sequentially and aborts remaining ones on first failure — a red apps/gateway run means web/testbot suites silently never execute that CI run | evidence: workspaces order gateway → testbot → web; nothing in ci.yml runs workspaces independently.
- file: apps/web/app/api/webhooks/creem/route.test.ts | line: 1122 | severity: nit | summary: Loud-skip justification rests on false premise — comment claims vitest config suppresses console.warn, but no vitest config sets `silent` | evidence: comment at :1122-1124 vs apps/web/vitest.config.mjs + apps/gateway/vitest.config.mjs (only environment/include/alias/setupFiles).
- file: apps/web/eslint.config.mjs | line: 1 | severity: nit | summary: apps/web nested eslint config + own `lint` script are dead under root flat-config gate — root `eslint .` never merges nested configs | evidence: root `"lint": "eslint . --max-warnings 0"` governed by root eslint.config.mjs (locked rules); apps/web `lint` + eslint.config.mjs (plain recommended, no locked rules) not invoked by any CI step.
- file: .github/workflows/deploy.yml | line: 39 | severity: nit | summary: Gate sequence exists in three hand-synced copies (root `ci` script, ci.yml, deploy.yml) — drift caught only by a human reading a comment | evidence: deploy.yml:39-41 + package.json:19 `ci` script vs ci.yml:37-48; none calls the other.
