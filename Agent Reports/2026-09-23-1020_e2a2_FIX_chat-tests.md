# Task Report: expansion-e2a2-chattests

## Status
SUCCESS

## Files Touched
- MODIFIED: apps/web/app/api/chat/route.test.ts (4 hunks only: import `buildPersonaPrompt` from `@corvus/ai`; three no-system-prompt pins updated to expect the leading `{ role: 'system', content: buildPersonaPrompt() }` message; old-contract guard replaced with a positive system-message-first assertion)

## Dependencies Added
None. No manifest edits, no installs (nothing run).

## Assumptions Made
- `buildPersonaPrompt()` with NO botName is the locked expectation, matching the E2a route implementation and the orchestrator spec report (`buildPersonaPrompt()` called bare in route.ts).
- Importing `@corvus/ai` in the test is acceptable (workspace package, same import the route already uses); the expected value is computed live rather than a hardcoded string copy, so the test pins presence/position, not a stale snapshot of the prompt text.

## Open Questions for Orchestrator
None.

## Public Interface Exposed
No interface changes. Test-only change; `POST /api/chat` shapes, status codes, SSE frames, and exported test hooks untouched. Behavioral assertion delta: the provider-bound message array is now expected to lead with the persona system message followed by the unchanged history+message tail.

## Known Limitations
- The new pins compute the expected system content via `buildPersonaPrompt()` at test time, so a change to the prompt text itself will not fail these tests — prompt-text locking remains covered by the `@corvus/ai` package's own tests.
- No live-model verification performed (mocked `chatStream` lane, no provider keys touched); no production-code change.
- No secrets read or printed; no git restore/commit commands used.

## Verification
- `npm run test --workspace @corvus/web -- app/api/chat/route.test.ts` → 33 passed / 0 failed.
- `npm run typecheck --workspace @corvus/web` → exit 0, zero errors.
- `npm run lint --workspace @corvus/web -- app/api/chat/route.test.ts` → exit 0, zero warnings.
