# Orchestrator SPEC — site↔motor köprüsü (Phase A: minimal live bridge)
Status: LOCKED SPEC pending founder approval — static translator maps prod specs to bot_runtime_config (6 kinds); Postgres-backed handlers; boot reader via startAll; guild deploy script; no JSON state.
Touched: CREATED this SPEC (grounds repo-map, oss-runtime, discord-locks briefs).
Note: Phase A tasks A1–A8; dashboard/security/fleet-throttle deferred to Phase B/pre-launch.
