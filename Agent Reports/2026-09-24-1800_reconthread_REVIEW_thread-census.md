# Task Report: recon-threadcensus-1800

## Status

**SUCCESS** — read-only census completed. All five cited lines re-derived against current bytes, render chains traced to DOM or proven absent, sweep coverage enumerated, LIVE/DEAD verdicts issued with channel arguments, scoped recipes written for the LIVE items.

**Headline:** the citation's five lines are all real, all still English, and **four of the five are LIVE residue** (channel provably reaches rendered DOM through the shared error-row path); **one (ATTACHMENTS_UNSUPPORTED) is DEAD as a page-sweep entry** and must not get one. None is covered by any existing Turkish-residue sweep; three have pre-existing *positive* English pins (unit or page level) that would go red on a translation — those are named below. No web research was needed (stated explicitly in Assumptions Made).

## Files Touched

- **CREATED: none.** The intended report file (`Agent Reports/2026-09-24-1800_reconthread_REVIEW_thread-census.md`) could not be written: this agent runs strictly read-only with no file-creation capability. The full report is this message; see Open Questions Q1.
- **MODIFIED: nothing.** No source, test, manifest, lockfile, env, or git state touched. HEAD verified `d9cf8d7`, tree UNCOMMITTED as handed over; every command run was read-only (grep/sed/git show/status).

## Dependencies Added

None. No install command was run.

## Assumptions Made

- **No web research needed, stated explicitly per RESEARCH FIRST.** This is a bytes-on-disk census: no library version, model name, API surface, or pricing fact is in play, so no live source was cited. Every fact below is a byte read out of this repo or a command's own exit code. Nothing intruded that required live research.
- **Path correction, re-derived not assumed:** there is no `apps/web/lib/use-chat-stream.ts`. The cited file lives at `apps/web/components/ui/use-chat-stream.ts` (imported by both chat pages from that path; `new/page.tsx:27`, `bots/[id]/page.tsx:26`). All line numbers below are re-derived against **current bytes** (HEAD `d9cf8d7` plus the uncommitted F7-wave edits); the citations were written against older bytes and have drifted by whole lines (`thread.ts` is 125 lines at HEAD, 155 now; the drift is the Turkish wave's comment blocks).
- **"Renders-to-DOM" is judged at jsdom level** through the real component path (`NewBotPage`/detail page → `ChatAssistantRow` → `ThinkingTrace`), per the F7N3 reviewer's disclosed instrument. No browser drive.
- The dead-entry rule applied is the one re-derived from the three context reports: **never install a residue entry without proving its channel is live** (F12B/F12C class; author PROBE 4 + reviewer P-premise dual counter-probes are the reference).

## Verification — the census

### 1. Line re-derivation (cited → current bytes, exact quoted content)

| Cited | Current | File | Exact content (quoted from current bytes) |
|---|---|---|---|
| `thread.ts:52` | `:52-53` | `apps/web/lib/chat/thread.ts` | `message:` / `typeof record.message === 'string' ? record.message : 'The reply stopped unexpectedly.',` — the fallback literal sits on `:53`, inside `toChatStreamEvent`'s error branch (`:49-55`) |
| `thread.ts:137` | `:138` | same | `return 'You are logged out — log in again, then press Retry.';` — first branch of `readHttpError` (`:136-147`) |
| `thread.ts:148` | `:142` and `:146` | same | `:142`: `return readRefusalMessage(payload) ?? 'The reply stopped unexpectedly. Try again.';` and `:146`: `return 'The reply stopped unexpectedly. Try again.';` — one string, two emission points of `readHttpError` (the cited `:148` was the single tail return before the Turkish-wave comments moved lines) |
| `use-chat-stream.ts:30` | `:30-31` | `apps/web/components/ui/use-chat-stream.ts` | `export const ATTACHMENTS_UNSUPPORTED =` / `'Image sending is not connected yet, so this turn was not sent. Remove the images and try again.';` — literal on `:31` |
| `use-chat-stream.ts:120` | `:120` (unchanged) | same | `error: 'The reply stopped unexpectedly. Try again.',` — inside `runStream`'s catch (`:115-122`) |

### 2. Per-line verdict table

Every string reaches DOM through the **same channel**: the value is written into a `ThreadRow.error` (or arrives as the server's own error-frame `message`), and `ChatAssistantRow` renders it at `apps/web/components/ui/chat-thread.tsx:36` — `<span>{message.error ?? 'Yanıt beklenmedik şekilde kesildi.'}</span>` — inside the thread `<ul aria-label="Yeni bot konuşması">` that mounts only when `messages.length > 0` (`apps/web/app/dashboard/new/page.tsx:471-472`, row at `:479`; detail page: `bots/[id]/page.tsx:1383`).

| # | Current line | String (owner file) | Renders to DOM? — chain | Covered by which sweep? | Entry verdict |
|---|---|---|---|---|---|
| 1 | `thread.ts:52-53` | `'The reply stopped unexpectedly.'` — fallback for a malformed `{t:'error'}` frame | **Yes, channel live.** `toChatStreamEvent` (`thread.ts:49-55`) ← `parseSseFrame` (`:59-71`) ← `use-chat-stream.ts:110` ← `applyStreamEvent` (`:74`: `error: event.message`) → row.error → `chat-thread.tsx:36` → DOM. **Trigger is latent, channel is live:** the only server producer (`app/api/chat/route.ts:448`) always passes `message:` (its literal is byte-identical English), so today this fallback fires only on a malformed frame — but the render path is the standard error row, and a harness pushing `frame({ t: 'error' })` renders it immediately. | **NONE.** `chat-thread.test.tsx:24-34` carries an English entry for the *component's* fallback slot (`:29`) but its sweep rows carry no caller prose (`:20-23` comment: `message.error` is data, excluded by design) — so the owner literal is not caught there. `page.test.tsx`: zero occurrences (grep count 0). | **LIVE** (mounted-state sweep inside an error repro `it`; see Recipe A). Cross-file: the byte-identical **server** literal at `route.ts:448` (other file) renders through the pass-through branch — a translation that skips it leaves visible English and the suite correctly red. |
| 2 | `thread.ts:138` | `'You are logged out — log in again, then press Retry.'` | **Yes — proven end-to-end on the detail page today.** `readHttpError` (`:136-147`) ← `use-chat-stream.ts:94` (`!response.ok`) → row.error → `chat-thread.tsx:36`. The existing detail test `bots/[id]/page.test.tsx:434` ('a 401 says logged-out in plain words…') drives a 401 and asserts the English at `:455` with the scope note *"The sentence is written by `lib/chat/thread.ts` (outside this task's scope, still English)"*. Same chain mounts on `/dashboard/new` (route returns `errorJson(401,'unauthorized')` at `route.ts:346`). | **NONE** as a Turkish-residue sweep; covered only as a **positive English pin** (`bots/[id]/page.test.tsx:455`) and at unit level (`lib/chat/thread.test.ts:84`, `:91`). | **LIVE** (Recipe B — the guard belongs in the existing detail `it`, whose render already exists). Byte locks that go red on translation: `thread.test.ts:84/:91`, detail `:455`. |
| 3 | `thread.ts:142` + `:146` | `'The reply stopped unexpectedly. Try again.'` | **Yes, channel live.** Same chain as item 2; fires when the chat response is `!ok` and the body is non-JSON (`:140-145` catch) or carries neither field (`{}`/`{detail:'x'}` → `readRefusalMessage` returns null). Real triggers: proxy/error-page bodies, `body: null` stubs. The existing `page.test.tsx:1755` repro stubs `{error:'chat blew up'}` — whitespace-bearing code → passes through `refusal.ts:71` — so that exact stub renders *caller prose*, **not** this owner string; a `{}` body is needed to render it. | **NONE.** Positive unit pins exist: `lib/chat/thread.test.ts:101`, `:143`, `:145` assert the English byte-equal. | **LIVE** (Recipe A, HTTP-error repro with a neither-field body; the existing stub must be varied or the entry cannot fire there). |
| 4 | `use-chat-stream.ts:30-31` | `'Image sending is not connected yet, so this turn was not sent. Remove the images and try again.'` (`ATTACHMENTS_UNSUPPORTED`) | **Never through the real UI on either page.** The composer refuses attachment-carrying submits first (`ai-chat-input.tsx:713-716` sets its notice and returns; `onSubmit` fires only with `attachments: []`, `:726-729`), and both pages wire `submit(value, meta.attachments)` (`new/page.tsx:536`, `bots/[id]/page.tsx:1044`). The hook's attachment branch (`:139-156`) is M-6 **defense in depth**, reachable only by a caller that bypasses the composer — which no page does. DOM chain, if ever reached: row.error → `chat-thread.tsx:36`. | **NONE as a sweep**; **positive unit pins** exist: `use-chat-stream.test.tsx:178` and `:202` assert `error: ATTACHMENTS_UNSUPPORTED` via the constant import (byte-equal, no retyped copy). Composer's *own* different sentence is pinned at `ai-chat-input.test.tsx:279` and by substring at `:361-362`. | **DEAD as a page-sweep entry — recommend NO entry.** The channel through the mounted page render is structurally unreachable (composer gate), so a `page.test.tsx` entry would be swept against a body that can never contain the string — the exact F12B/F12C dead-entry class reproduced by PROBE 4/P-premise (full English ships 49/49 green on the dead design). The existing positive unit pin is the right altitude for an M-6 branch. |
| 5 | `use-chat-stream.ts:120` | `'The reply stopped unexpectedly. Try again.'` | **Yes, channel live.** `runStream` catch on fetch reject/reader throw (`:115-122`, guarded by `!controller.signal.aborted`) → row.error → `chat-thread.tsx:36`. The existing repro `page.test.tsx:1787` ('repro: composer unlocks on a chat network throw', fetch rejects `'connection reset'`) drives exactly this path and the row renders — the test asserts only composer unlock, never the text. | **NONE.** Zero occurrences of any of these strings in `page.test.tsx` (grep count 0, exit 1) and none retyped in `use-chat-stream.test.tsx`. | **LIVE** (Recipe A, network-throw repro; add the sweep call inside the existing `it`). |

### 3. Class-not-caller — every sibling user-visible English literal on a render path in the two files

Complete scan of both files; these are all the render-path literals. Fix-or-exclude with reason:

- `thread.ts:52-53` — cited (item 1). Fix: Recipe A.
- `thread.ts:138` — cited (item 2). Fix: Recipe B.
- `thread.ts:142`, `thread.ts:146` — cited (item 3, one string, two emissions). Fix: Recipe A.
- `use-chat-stream.ts:30-31` — cited (item 4). **Excluded from any residue list** — dead channel (F12B/F12C + P-premise); keep the existing positive unit pins.
- `use-chat-stream.ts:120` — cited (item 5). Fix: Recipe A.
- **Excluded, named explicitly:** `thread.ts`'s remaining content is types, numeric/identity helpers and comments — no other string reaches DOM (`formatCredits` returns digits; `chatBotId` returns null; `stitchBrief`/`threadHistory` produce model-traveling content, not copy). `use-chat-stream.ts` renders nothing itself (its header: "No rendering here") and its only other literals are `msg-${n}` ids — never rendered.
- **Same class, outside the two files, named for the fix wave's boundary:** (a) `app/api/chat/route.ts:448` — the server's byte-identical `'The reply stopped unexpectedly.'`, renders through item 1's pass-through; load-bearing for Recipe A. (b) `ai-chat-input.tsx:31` (`ATTACHMENT_SEND_UNSUPPORTED`, 'Image sending is not connected yet, *so nothing was sent*…') — a genuinely live English render on both chat pages (role=alert at `ai-chat-input.tsx:866-873`), pinned positively at `ai-chat-input.test.tsx:279/:361/:362`; excluded from the F7N3 page sweep by its own documented rule ("Shared components (composer…) are out of scope — they serve the bot-detail page too", `page.test.tsx:43-45`). (c) The English `trial_expired` sentence hard-coded at `app/api/chat/route.ts:88` and pinned positively on the new page at `page.test.tsx:1625-1682` — documented KNOWN DRIFT in `lib/bots.ts:73-76`. All three are the same residue class in files this census does not own.

### 4. Scoped recipes — LIVE items only

Per F12C precedent: no new `it` block; per-branch list beside the existing ones; sweep only where the branch mounts; mutation-proof one entry at a time with every other guard neutralized.

**Recipe A — the two "reply stopped" strings (items 1, 3, 5).** Home: `apps/web/app/dashboard/new/page.test.tsx`, a new `ERROR_RESIDUE: [string, string][]` beside `THINKING_RESIDUE`/`PARKED_RESIDUE` (`:73-77`), entries `['error frame', 'The reply stopped unexpectedly.']` and `['error fallback', 'The reply stopped unexpectedly. Try again.']` (the short entry subsumes the long one — keep both for per-entry attribution, the F7N3 per-branch-list pattern). Sweep via `expectNoEnglishResidue(ERROR_RESIDUE)` inside **existing** `it` blocks where the row mounts:
- `:1729` 'repro: composer unlocks after an error event' — for item 1, the render under test must be the **fallback branch**: push `frame({ t: 'error' })` (no `message`) so `thread.ts:52-53` supplies the text; its current `frame({ t: 'error', message: 'Upstream blew up.' })` renders caller prose and cannot fire the entry.
- `:1755` 'repro: composer unlocks on a chat HTTP error' — for item 3, the stub body must carry neither field (`json: async () => ({})`), since the current `{error:'chat blew up'}` renders caller prose, not the owner string.
- `:1787` 'repro: composer unlocks on a chat network throw' — item 5; already drives `use-chat-stream.ts:120` exactly; add the sweep call after the error row mounts.
- Mutation proof (after translating the literals to Turkish): revert one literal at a time → exactly one red at the new call site with the remaining pins neutralized (no direct English pins for these strings exist in `page.test.tsx` — grep count 0 — so neutralization is trivial); control: unmutated copy green. Add a positive Turkish pin beside each call site (the `:522` precedent) so the branch's copy is asserted, not merely un-English.
- Load-bearing cross-file consequence: `route.ts:448` and the `thread.test.ts:101/:143/:145` byte locks must move in the same wave, or the suite stays red / the visible string survives.

**Recipe B — the 401 line (item 2).** The mount already exists in `bots/[id]/page.test.tsx:434` ('a 401 says logged-out in plain words and Tekrar dene re-issues after login'): after translating `thread.ts:138`, replace the positive English assertion at `:455` with the Turkish bytes plus a negative `expectNoEnglishResidue`-style check (the detail suite has no residue helper yet — install the entry pair `['logged-out line', 'You are logged out — log in again, then press Retry.']` in that `it`, no new `it` block). Byte locks to update in the same change: `lib/chat/thread.test.ts:84/:91`. Cross-page note: this string renders on `/dashboard/new` too; a new-page 401 repro is optional redundancy, not required for a live channel.

**DEAD item (4) — explicitly recommend NO entry.** Cite F12B/F12C and the dual counter-probe precedent (author PROBE 4 + reviewer P-premise: dead design + full English = 49/49 green). The composer gate (`ai-chat-input.tsx:713-716`) makes the hook branch structurally unreachable through the real UI on both pages; a page-level entry could never fire; the existing positive unit pins (`use-chat-stream.test.tsx:178/:202`, constant-imported, byte-equal) already lock the bytes at the correct altitude.

### 5. D-004 angle (one line)

All five strings are English-only **by documented decision** — D-004 (`Docs/DECISIONS.md:118-138`, founder, 2026-09-07, one-way, "English-only product, docs, marketing, and support") — and are "residue" only relative to the later F7 Turkish wave's own standard for this page's copy; labeled as such, not relitigated.

## Open Questions for Orchestrator

1. **BLOCKING (process) — the report file was not written.** This agent's role is strictly read-only with no file-creation tool, so the required artifact `Agent Reports/2026-09-24-1800_reconthread_REVIEW_thread-census.md` does not exist on disk; the complete report text is the message above. Ask: save it verbatim under that name (orchestrator-saved, as was done for `recon-verdict500`), or re-issue this census to a write-enabled agent.
2. **Scope decision — the fix is a translation wave crossing file boundaries.** Making items 1/2/3/5 Turkish touches `route.ts:448`, `lib/chat/thread.test.ts:84/:91/:101/:143/:145`, and `bots/[id]/page.test.tsx:455` — owners outside the two cited files — and lands Turkish error copy inside the detail page's still-English shell (already recorded in `chat-thread.tsx:5-12`). Founder/orchestrator call, D-004 conflict as recorded.
3. **Sibling-class boundary** — `ai-chat-input.tsx:31` (live English alert on both pages, positively pinned) and the documented trial-drift (`route.ts:88` vs `lib/bots.ts:78`) are the same residue class in unowned files: fold into the fix wave's list or keep excluded per the F7N3 shared-component rule?
4. **Carried forward unchanged:** F7N3 review's non-blocking items (`:523` vacuous negative pin; run-B prose gloss) remain open and untouched by this census.

## Public Interface Exposed

None — no production symbol, route, prop, or type changed, and no test interface changed; this task wrote nothing. Observed test-internal surfaces relevant to the recipes (all pre-existing): `expectNoEnglishResidue(extra)` helper `page.test.tsx:390-395`, `ENGLISH_RESIDUE` 16 entries `:49-66`, `THINKING_RESIDUE` `:73`, `PARKED_RESIDUE` `:74-77`, call sites `:469`/`:497`/`:524`; `chat-thread.test.tsx` residue list `:24-34` (sweep `it` at `:139-162`); detail suite `RETIRED_COPY` `:62` (source-scope sweep, `:689`/`:695`) and positive pins `:379`/`:455`; `use-chat-stream.test.tsx` positive pins `:178`/`:202`.

## Known Limitations

- **jsdom/bytes level, no browser drive** — true. Every verdict is derived from file bytes, grep exit codes, `git show` of HEAD, and the suites' own existing assertions; no app was started and no real browser flow was driven.
- **LIVE verdicts are structural, not mutation-proven by me.** I ran no mutations (read-only). Each LIVE verdict rests on a traced render chain plus an existing harness that already mounts the exact branch (`:1729`/`:1755`/`:1787`, detail `:434`); the English-revert mutation proof belongs to the fix wave per Recipe A/B.
- **Line numbers describe the current uncommitted tree** (HEAD `d9cf8d7` + F7-wave edits); the two census files themselves are `M` in git status (diff --stat: thread.ts +35/-…, use-chat-stream.ts +38, chat-thread.tsx +18) — numbers will drift if the tree changes.
- **Trigger reachability vs channel liveness is distinguished but production-frequency is unmeasured** — e.g. item 1's fallback fires only on a malformed error frame (today's only producer always sends `message`), item 3's on non-JSON/neither-field bodies; I did not measure production frequency, only that the channels are structurally live and harness-provable.
- **The detail suite's chat-network-throw coverage was not fully enumerated** (its throw repros I read target the interview button); Recipe B deliberately homes the 401 guard where the render already exists rather than assuming a missing harness.

---

**Summary for orchestrator:** Census complete, read-only, nothing modified. Five cited lines re-derived (drift: `use-chat-stream.ts` is at `components/ui/`, `thread.ts:52→52-53, 137→138, 148→142+146`). Four items are LIVE residue on the shared error-row channel (`chat-thread.tsx:36` ← `use-chat-stream.ts` ← `thread.ts`, mounted only with a turn at `page.tsx:471-483`) with scoped recipes: two "reply stopped" strings via a new `ERROR_RESIDUE` list swept inside the three existing error-repro `it`s in `page.test.tsx` (`:1729` variant with `frame({t:'error'})`, `:1755` with a neither-field body, `:1787` as-is), and the 401 line inside the existing detail `it` at `bots/[id]/page.test.tsx:434/:455`. Item 4 (`ATTACHMENTS_UNSUPPORTED`) is DEAD as a page-sweep entry — composer gate makes its channel structurally unreachable; recommend NO entry per F12B/F12C + P-premise, keeping the existing positive unit pins. None covered by any existing sweep; byte locks (`thread.test.ts:84/:91/:101/:143/:145`, detail `:455`, server literal `route.ts:448`) must move with any translation. All five are D-004 English-by-decision. The report file could not be created (read-only role) — Q1 asks you to save the text above as `Agent Reports/2026-09-24-1800_reconthread_REVIEW_thread-census.md`.
