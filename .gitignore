node_modules/
dist/
.next/
coverage/
*.tsbuildinfo
.env
.env.*
!.env.example
# Local-only dev runtime state (D-089: votes live in a file, lost on box move)
.pick-votes/
# Founder-supplied unlicensed harvest (D-058): never enters version control
pyrzm.zip
# Local DB dump backups (pg-backup compose profile): local artifacts, never source
infra/compose/backups/
*.dump
# Test/browser run artifacts: local junk, never source
apps/web/.vitest/
.playwright-mcp/
