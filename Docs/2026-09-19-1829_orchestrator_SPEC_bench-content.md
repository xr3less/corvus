# SPEC: bench-content — builder content correctness, 4 live re-runs + spec-detail snapshot + line-by-line grade
Status: SPEC ordered 4 live re-runs (C1-C4) + FULL spec JSON snapshot + line-by-line R1-R5 grade, cost ~$0.01 abort >$1.
Touched: spec + 2 runner reports + 1 content scorecard (box transient rows only)
Note: Corrects bench-quality R2/R3 gap; no publish, no key handling, no code changes; stop on premium-lane drift.
