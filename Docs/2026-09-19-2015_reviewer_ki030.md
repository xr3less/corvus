# Task Report: reviewer-ki030-honesty-wave

## Status

PARTIAL

Pages are honest; tests and formatting are not. Every KI-030 page file I checked renders the locked honest copy and the fail-empty contract holds in code, and repo `tsc` is clean. But the wave is not shippable as-is: ki030-c's three test files were never updated (they assert the old dishonest copy and now fail heavily), `layout.test.tsx` (outside every agent's scope) still asserts the removed Pro pill and credits meter, prettier fails on the three ki030-c page files, and the dashboard home retains three residual fake-data patterns (fabricated This-Week activity, static pre-flight rows, `(example)` aria markers) that the fix wave must rule on.

## Files Touched

- CREATED: Docs/2026-09-19-2015_reviewer_ki030.md (this report)
- READ-ONLY: Docs/2026-09-19-2001_orchestrator_SPEC_ki030-honesty.md
- READ-ONLY: Docs/Teknik_Borc/KI-030_honesty.md
- READ-ONLY: Docs/2026-09-19-2015_ki030-a_MODIFY_landing-copy.md
- READ-ONLY: Docs/2026-09-19-2015_ki030-b_MODIFY_rail-home.md
- READ-ONLY: Docs/2026-09-19-2015_ki030-d_MODIFY_gallery.md
- READ-ONLY: Docs/2026-09-19-2015_ki030-e_MODIFY_legal.md
- READ-ONLY: apps/web/app/page.tsx, apps/web/app/page.test.tsx
- READ-ONLY: apps/web/components/ui/dashboard-rail.tsx, apps/web/components/ui/dashboard-rail.test.tsx
- READ-ONLY: apps/web/lib/bots.ts
- READ-ONLY: apps/web/app/dashboard/page.tsx, apps/web/app/dashboard/page.test.tsx
- READ-ONLY: apps/web/app/dashboard/bots/page.tsx, apps/web/app/dashboard/bots/page.test.tsx
- READ-ONLY: apps/web/app/dashboard/bots/[id]/page.tsx, apps/web/app/dashboard/bots/[id]/page.test.tsx
- READ-ONLY: apps/web/app/dashboard/new/page.tsx, apps/web/app/dashboard/new/page.test.tsx
- READ-ONLY: apps/web/app/dashboard/layout.test.tsx
- READ-ONLY: apps/web/app/gallery/page.tsx, apps/web/app/gallery/page.test.tsx
- READ-ONLY: apps/web/app/terms/page.tsx, apps/web/app/terms/page.test.tsx
- READ-ONLY: apps/web/app/privacy/page.tsx, apps/web/app/privacy/page.test.tsx
- READ-ONLY: apps/web/app/demo/page.tsx (line 90 only, via grep)

## Dependencies Added

- None.

## Assumptions Made

- Curly (`’`) vs straight (`'`) apostrophes render identically; I treat `isn’t`/`aren’t`/`server’s` in JSX as matching the spec's straight-quote locks. Tests using substring fragments (`aren`, `isn't live yet`) pass either way.
- The project's real vitest runner is `npx vitest run --config apps/web/vitest.config.mjs` from repo root (per ki030-b/e notes). Bare `npx vitest run <path>` failures (alias/jsdom) are harness invocation detail, not code defects.
- The single landing-test console-error failure (`HTMLCanvasElement's getContext()`, jsdom has no canvas package) is environmental noise from the pre-existing `VelarisCanvas`, not a copy regression: every copy assertion in that test passed.
- ki030-c tests render mock ids (`bot-3`, no injected `bots` prop) because they were written when mock fallback existed; under the new fail-empty contract those render honest empty states, so their failures are stale-test fallout, not page bugs.

## Open Questions for Orchestrator

1. `fetchBots()` failure snapshots keep `source: 'mock'` with `bots: []` (ki030-b's type-compatible choice). Semantically odd but contract-legal (spec locks only `[]`, not `source`). Confirm keeping it, or lock a new `source`/`isLive` shape in the fix wave (type change touches sibling consumers — your call).
2. Templates lock: ki030-a put `Templates preview — start free while in preview` on the ghost-CTA link (`apps/web/app/page.tsx:499`) and kept the H2 `Start from a template` (`:492`). The H2 as-is makes no live/paid promise. Confirm this matches spec intent (I recommend yes, no change).
3. Dashboard-home residuals ki030-b deliberately left per stop rules: (a) This-Week panel fabricates activity via `activityFor(bots[0])` when any bot exists; (b) static `PREFLIGHT_ROWS` presented unmarked; (c) `Get started (2/4) (example)` aria markers kept. Decide in fix wave: honest-label or remove. Item (a) is the most dishonest-looking and I recommend fixing it.
4. Composer cost lines (`About 1.1 credits per change · platform failures retry free.` at `new/page.tsx:241` and `[id]/page.tsx:789`) state a credit price with no `Planned:`/`(not enforced yet)` marking, unlike the landing FAQ credits line. If chat credits are not enforced yet, mark them; if they are enforced, confirm and close.
5. `layout.test.tsx` belonged to no agent's scope and now fails (asserts removed Pro/credits). Assign it to the fix wave explicitly (one-file test update to the honest rail).
6. Curly-vs-straight quotes: if any future byte-level checker compares locked strings, normalize quotes first or it will false-fail on `’`.

## Public Interface Exposed

- None (review only; no code, no exports, no routes).

## Known Limitations

- Read-only review: I ran gates (tsc, eslint, prettier --check, vitest) but executed no --write, no installs, no git commands, no network calls, no browser run. "Works" here means gates + code reading, not a human click-through of the running app.
- I did not re-verify API routes, gateway, packages, or infra beyond confirming via `git diff --name-only` that none changed.
- Prettier failures recorded as-is; I did not fix formatting (out of scope for reviewer).
- The landing canvas jsdom failure was not bisected to a commit; attributed to environment because the copy assertions pass and the error names a jsdom-unimplemented API on a pre-existing canvas component.

## VERDICT

Conventions: `path:line` below is working-tree state at review time. "Page" = honest, "Test" = stale unless noted.

| Leg                                                                               | Verdict                                               | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --------------------------------------------------------------------------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (1) Spec adherence — landing (ki030-a)                                            | PASS (2 notes)                                        | Uniform token sentence verbatim `app/page.tsx:42`; bento `page.tsx:282-284`; tagline renders `page.tsx:904-906` (`&apos;` encoding, identical render); badge+metadata `free preview` `:10`,`:199`; hero+Starter `Start building free` + `/dashboard` hrefs + sub-note `:213-221`,`:738-741`; Pro/Studio disabled buttons, no href `:790-797`,`:842-849`; `Cancel anytime` deleted (grep: zero matches); pricing lede `:693-695`; `Planned:` on numbered entitlement lines `:717`,`:721`,`:725`,`:729`,`:765`,`:769`,`:773`,`:777`,`:781`,`:818`,`:822`,`:826`; FAQ nap `:46`, credits `Planned:`+`(not enforced yet)` `:54`, scan future-tense `:58` (leading `No. ` prefix added — acceptable grammar fit, flagged not failed); uptime `(planned)` throughout `:357`,`:364`,`:367`,`:375`,`:378`,`:386`,`:389`,`:403`,`:408`,`:412`,`:416`; footer Learn/Product/Company honest `:910-952`, community entries spans (test `:88-96` guards). Notes: (i) curly `’` in FAQ nap/scan vs spec straight quote — render-identical; (ii) templates lock on ghost-CTA link `:499`, H2 kept — see OQ2.                       |
| (1) Spec adherence — rail+home+bots.ts (ki030-b)                                  | PASS (1 residual set)                                 | Rail: Pro pill gone, credits line+meter gone, disabled `Upgrade · Coming soon` kept `dashboard-rail.tsx:79-88`. `TRIAL_DEAL` locked `lib/bots.ts:43`. Home: `Follow your bot from draft to saved version.` `:157`; stat `—`/`No data yet` `:86-101`; `No bots yet — describe your first bot.` panel + `/dashboard/new` link `:171-178`; week fallback `:199`; workspace trial line `:224`, honest Upgrade `:226-234`. Residuals left deliberately (see OQ3): This-Week `activityFor` fabrication `:103-107`; static pre-flight `:36-40`,`:207-217`; `(example)` aria labels `:117`,`:124` + comment `:24`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| (1) Spec adherence — bots list/detail/new (ki030-c, no report; reviewed directly) | PASS on pages, FAIL on tests/format (see Regressions) | List: `No data yet` vs real counts `:124-128`; empty `No bots yet — describe your first bot.` + link `:183-191`; counts omitted when absent `:215-222`; build-progress line `Follow your bot from draft to saved version.` `:278`. Detail: `Save version` `:887`; success `Version ${n} saved. Your bot isn’t live on Discord yet.` `:432-437`; retry `press Save version to retry` `:454`; install `(shared test app — your own bot install isn’t wired yet)` `:917`; trial line `:868`; overview honest fallback `:969-973` (no mock specs). New: locked draft-then-save + `Going live on Discord isn’t wired yet.` `:184-187`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| (1) Spec adherence — gallery (ki030-d)                                            | PASS                                                  | Starts `[]` `:109`; `Templates unavailable — try again.` `:307`; real API forks `:365-367`; `Add to Discord (shared test app)` `:383` (post-fork invite link; Fork button itself `:407-414` — matches spec's button-label variant per report assumption); forked 401/error honesty `:387-398`. No `MOCK_TEMPLATES` (deleted; grep confirms only spec/docs mention it).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| (1) Spec adherence — legal (ki030-e)                                              | PASS                                                  | Terms billing lead verbatim `:83-85`; `Planned:` Trial/Paid/Credits/Lapses `:89`,`:93`,`:97`,`:103`; no `billed through Creem`, no dashboard-cancel, no Creem-refund (test `:61-64` guards); short-version trial marked planned `:44`. Privacy Creem lines both carry `isn't live yet` `:79-81`,`:117-119`; TRUE token sentence untouched `:102-104`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| (2) Contract integrity — `fetchBots()` failure → `[]`                             | PASS                                                  | `lib/bots.ts:218-235`: 401/non-ok/malformed/network all return `{ bots: [], … }`, never throws; 2xx array (incl. empty) is the only live path `:231`. Pages branch on `bots.length === 0`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| (2) Contract integrity — exports preserved                                        | PASS                                                  | Full export list intact (`MOCK_BOTS` `:37`, `TRIAL_DEAL` `:43`, `CREDITS_USED/CREDITS_TOTAL` `:78-79`, `mockSpecFor` `:121`, `BotSource` `:171`, `mockBotsSnapshot` `:183`, `toDisplayBot`, `readLiveBotRows`, `fetchBots`, `resolveBotId`, types). Nothing removed/renamed. No page outside tests imports `mockSpecFor`/`mockBotsSnapshot` (grep: definitions + stale test import only).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| (2) Contract integrity — repo tsc clean                                           | PASS                                                  | `npx tsc --noEmit -p apps/web/tsconfig.json` from repo root: clean, zero output. (ki030-b/d's "red tsc" reports are stale — ki030-c finished its rewrite; the `MOCK_BOTS`/`BotSource` import errors they recorded are gone.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| (3) No regressions — change scope                                                 | PASS                                                  | `git diff --name-only` = exactly the 5 agents' rows (landing, rail+test, bots.ts, dashboard home+test, bots list/detail/new pages, gallery page+test, terms/privacy pages+tests) + `Docs/PLAN.md`. No API/gateway/packages/infra/auth/chat/builder/preflight/templates-API/bots-API/manifest/lockfile changes. No `pryzm/*`, no `demo/*`. `demo/page.tsx:90` honest marker untouched (grep-verified).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| (3) No regressions — owned vitest files                                           | PARTIAL (see row below)                               | Batch run (project config): 53/54 green across `page`, `dashboard-rail`, `dashboard/page`, `gallery/page`, `terms/page`, `privacy/page`. The 1 failure is landing `page.test.tsx:42` console-error assertion tripped solely by jsdom `HTMLCanvasElement.getContext() not implemented` (VelarisCanvas, pre-existing) — all copy assertions in that test passed. ki030-a claimed 4/4 green from `apps/web`; I could not reproduce green (3/4 alone, same canvas error) — environmental, but the orchestrator should know the claim did not reproduce.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| (3) No regressions — stale-test fallout (fix wave required)                       | FAIL                                                  | ki030-c's three test files were NOT modified and now fail against the honest pages: `bots/page.test.tsx` 13/21 fail (asserts `Your bots (example)` `:86`,`:98`,`:243`,`:259`; `snapshot.bots toBe(MOCK_BOTS)` `:322`; old empty copy `Connect your first server`/`Live 0 / Trial 0 / Off 0` `:170-183`; mock-fallback 401/500 tests `:232-262`; card tests expecting Study Hall/Draft Arena/`Published v12` without injected props). `[id]/page.test.tsx` majority fail (old `TRIAL_LINE` `:14`; `Publish` `:147+`; `What this bot does (example)` `:175`,`:181`,`:187`,`:377`; `Example` activity `:166`,`:183`; `Published v7/v5` `:394`,`:554`,`:837`; mock-id renders without injected bots now hit unknown-id empty). `new/page.test.tsx` 1/12 fail (old `CREATION_SUB` `then it goes live.` `:6` vs honest page). `dashboard/layout.test.tsx` 1/4 fail (asserts `Pro` `:43`, `Credits 82/100 · 18 left` `:60`, credits progressbar `:61` — file owned by nobody).                                                                                                                                             |
| (3) Gates — eslint                                                                | PASS                                                  | `eslint --max-warnings 0` on all 17 wave files: clean, zero output.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| (3) Gates — prettier                                                              | FAIL (ki030-c files only)                             | `prettier --check` on wave files: 3 warns — `dashboard/bots/page.tsx`, `dashboard/bots/[id]/page.tsx`, `dashboard/new/page.tsx` (all ki030-c, whose agent died before running gates). All other 14 files clean. Fix = one `prettier --write` on those 3 files in the fix wave.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| (4) Secrecy                                                                       | PASS                                                  | Sweeps for `sk-…`, private keys, `password/api-key` assignments: zero matches in `apps/web`. No token/password/key values introduced in the wave diff (copy-only + empty-state logic).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| (5) Forbidden-pattern sweep                                                       | PASS on pages (2 flags for fix wave)                  | Zero remaining in pages: no `Start 3-Day Free Trial`/`Upgrade to Pro`/`Select Studio Plan`/`Cancel anytime` (grep clean); no `Published v` note on detail (only API-route internals + stale tests); no `then it goes live` in pages (`new/page.tsx:186` honest); no `draft to live` in pages (only a stale-test negative assertion); no `412 forks` fabrication in gallery page (real API rows only); no `CREDITS_USED/TOTAL` render (rail/home deleted; constants export-only); no paid CTA with commerce href (Pro/Studio/Upgrade all disabled buttons); no unmarked `Planned` commerce (all entitlement numbers + pricePers + legal marked); `(example)` theater removed from visible copy except dashboard-home aria labels + comment (flagged, OQ3). Flags: (i) home This-Week `activityFor` renders fabricated `Published X v12`/credit suffixes whenever bots exist — the largest remaining fake-data pattern; (ii) `pryzm/page.tsx` still contains Polar billing + `(example)` stats, but it is parked/404-guarded and explicitly out of scope — do not touch, noted only so nobody mistakes it for a miss. |
