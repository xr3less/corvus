# SCREENSHOTS — INDEX

## Status: LIVE

> One line per image, so an AI (which can't "see" the folder by skimming filenames alone) knows what each capture shows and which doc it belongs to — without opening every file. Append a row when you add an image.

---

## Naming rule

`YYYY-MM-DD_area_state.png` — e.g. `2026-01-01_canvas_empty-state.png`, `2026-02-10_pricing_mobile.png`. Date first so the folder sorts chronologically; area + state so the name is self-describing.

---

## Index

| File | Date | What it shows | Linked doc |
|---|---|---|---|
| *(empty — no captures yet. First screenshot fills row one per the naming rule above.)* | | | |

> Example (delete when real ones exist):
> | `2026-01-01_canvas_empty-state.png` | 2026-01-01 | empty drawing canvas, design reference | `04_design_language.md §5` |
