# Task Report: ki030-e-legal

## Status

SUCCESS

## Files Touched

- MODIFIED: apps/web/app/terms/page.tsx
- MODIFIED: apps/web/app/terms/page.test.tsx
- MODIFIED: apps/web/app/privacy/page.tsx
- MODIFIED: apps/web/app/privacy/page.test.tsx
- CREATED: Docs/2026-09-19-2015_ki030-e_MODIFY_legal.md (this report)

## Dependencies Added

- None.

## Assumptions Made

- Per the spec's "prefer removal" instruction, the dashboard-cancel and Creem-refund claims were REMOVED (not marked `planned`). Removal did not break page structure: the `Plans, trial, and billing` H2 and section list survive, and no test asserts the removed items.
- Terms billing block leads with the spec's locked sentence verbatim: `Payments aren't live yet — there is no checkout, no charges, no refill packs, and no dashboard cancel, because there is nothing to cancel. Below is what we intend to sell:` (stored with `&apos;` encoding, renders identically).
- Plan names/prices ($10 Pro / $29 Studio / $5 refill pack of 1,000 credits / 90 days / 12 months / naps) survive with `Planned: ` labels.
- Privacy Creem lines use a grammatically-fitting equivalent carrying both halves (future processor + not-live-yet): `Payments will run through Creem when billing ships — it isn't live yet.` (second occurrence capitalized after the `Creem —` lead-in with an em dash; both halves present in both lines).
- Terms short-version trial bullet was also made honest (`Billing isn't live yet. A 3-day trial with full access and no card is planned. ...`) since it restated the same live-trial promise; consistent with the billing block, no locked string violated.
- `prettier --write` on the two page files only reflowed JSX line-wrapping (verified via `prettier | diff` before writing — wrapping only, no wording changes).

## Open Questions for Orchestrator

- None blocking. One harness note for the reviewer: bare `npx vitest run <path>` from the repo root uses the root vitest (v3.2.7, no jsdom) and fails all 9 tests with `document is not defined` — pre-existing config layout, unrelated to this change. The project's real runner (`apps/web` vitest v5 with `vitest.config.mjs` jsdom env) is green 9/9.

## Public Interface Exposed

- None. Both routes remain default-exported static Next.js pages with unchanged section structure and footer links. No new exports, props, or endpoints.

## Known Limitations

- No checkout, cancel, or refund surfaces were created — out of scope; the pages now honestly say billing isn't live.
- Privacy TRUE token sentence (`When you connect a bot, its token is encrypted before it is saved. Only the part of our system that runs your bot can unlock it.`) kept verbatim — verified untouched at `apps/web/app/privacy/page.tsx:102-103`.
- Gates: `npx tsc --noEmit -p apps/web/tsconfig.json` clean; `eslint --max-warnings 0` on all 4 owned files clean; `prettier --check` on all 4 owned files clean (after one `--write` reflow pass on the 2 page files); owned vitest files green 9/9 when run under `apps/web` (2 root-runner attempts failed on environment only, recorded above, then green on the correct runner).
