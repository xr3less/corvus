# SPEC: stage-b — live Discord proof on real trial guild (throwaway bot, honest scope)
Status: SPEC ordered live Discord proof (throwaway timeout bot, ≤30min online, pointer + preflight + cleanup), max 1 builder call, abort >$1.
Touched: spec + 1 runner report (box transient rows only, all deleted)
Note: Provable: login/READY/guild/presence/pointer/preflight only — behavior execution NOT provable (no interpreter); gateway recreate FORBIDDEN; token lengths only, founder deletes Discord app.
