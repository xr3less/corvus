# 07 — Folder Structure & Standards

## Status: DRAFT (filled 2026-09-07, locked D-018)

> The repository layout, naming conventions, coding standards, and — per `GLOBAL_RULES.md §4.3` — the LIVE map of the entire file structure: what every significant file/folder is and what it's for. When the structure changes, this doc changes in the same task. A reader without code access should understand the project's shape from this file alone.

---

## 1. Repo layout (the file-structure map)

> LOCKED D-018 (single repo `corvus`, `npm workspaces`, no Turborepo). Keep this tree current — same task that changes structure updates this map.

```
corvus/ (repo root)
├── apps/web/               # Next.js 16: landing + dashboard + panel interview + gallery + demo window
├── apps/gateway/           # Node 24 + discord.js v14: N-token host + spec interpreter + supervisor/self-heal
├── packages/spec/          # shared TS: versioned spec zod schemas + JSONB types + drift tests (the contract)
├── packages/ai/            # shared TS: single lane definition (lanes + router + cost + builder prompt) for web + gateway (D-128)
├── infra/compose/          # docker-compose.yml + pg_dump sidecar + Caddy + Uptime Kuma (separate VPS)
├── .env.example            # all secrets documented (GLM/OpenRouter, Postgres, Creem, Discord app) — no real values
├── .github/workflows/      # ci.yml (typecheck/lint/vitest/drift/eval gate) + deploy.yml (GHCR -> SSH compose pull)
└── README.md
```

| Path                                                                                                                  | What it's for                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/web/`                                                                                                           | All UI. Landing static/ISR; dashboard force-dynamic (never cache user data)                                                                                                                                                                                                                                                                                                                                         |
| `apps/web/app/interview                                                                                               | gallery/`                                                                                                                                                                                                                                                                                                                                                                                                           | Interview wired to real API (D-124, mock fallback); gallery wired to list/fork APIs (D-122/D-124, mock fallback) |
| `apps/web/app/dashboard/`                                                                                             | Home (overview only) + `bots/` list page. Subpages: `new/` (creation chat) + `bots/[id]/` (detail). Surface rows below.                                                                                                                                                                                                                                                                                             |
| `apps/web/app/dashboard/layout.tsx` + `layout.module.css`                                                             | Shared shell + rail + ambient + main for all dashboard routes (links + active rule + Suspense; D-119)                                                                                                                                                                                                                                                                                                               |
| `apps/web/lib/bots.ts`                                                                                                | Shared mock bot data (bots, activity, preflight, specs) until backend binding (D-118)                                                                                                                                                                                                                                                                                                                               |
| `apps/web/lib/chat/thread.ts`                                                                                         | Shared chat primitives (rows/SSE/history/error/botId; D-118)                                                                                                                                                                                                                                                                                                                                                        |
| `apps/web/components/ui/`                                                                                             | Shared component kit (D-123 rule: one rail, one chat kit — no route-local copies): `dashboard-rail.*` (single rail for dashboard + gallery) + `thinking-trace.*` + `chat-thread.*` + `use-chat-stream.ts` + `ai-chat-input.tsx` (Auto-only composer) + misc primitives                                                                                                                                              |
| `apps/web/app/page.tsx` + `app/landing.module.css` + `app/landing-islands.tsx`                                        | Production homepage (D-120 Antigravity port; Velaris canvas + Lenis islands; hero asset `public/landing/dashboard-hero.jpg`)                                                                                                                                                                                                                                                                                        |
| `apps/web/public/clone-pryzm/`                                                                                        | INTERIM verbatim Pryzm copy 2026-09-11 (D-058 founder override: html+68 assets byte-identical, quarantined DO-NOT-SHIP ref). KEPT 2026-09-13 (D-097 cleanup): live `/pryzm` still serves fonts/images from here (KI-011) — delete only after `/pryzm` re-adapted                                                                                                                                                    |
| `apps/web/app/pryzm/`                                                                                                 | 1:1 Next.js port of the clone 2026-09-11 (D-059: JSX+CSS Modules, lenis npm + native fallback, Pryzm copy interim; taste-gated)                                                                                                                                                                                                                                                                                     |
| `Antigravity/`                                                                                                        | Founder-side design sandbox (single-file Tailwind CDN pages; standalone files, not a workspace). `index.html` landing (ported to production `/` as D-120; sandbox kept as reference); `dashboard.html` sandbox (port wiring tracked as KI-010); `*.backup-*.html` = pre-wave copies (delete after first commit).                                                                                                    |
| `apps/gateway/`                                                                                                       | All bots. Owns tokens (encrypted), spec interpreter, pg-boss workers, supervisor                                                                                                                                                                                                                                                                                                                                    |
| `apps/gateway/src/db/`                                                                                                | Drizzle schema + createDb factory + drizzle.config + drizzle/0001_init.sql (bots + user_records, SPEC §4 contract tables)                                                                                                                                                                                                                                                                                           |
| `apps/gateway/src/db/v11.ts` + `drizzle/0002_v11.sql`                                                                 | V1-1 tables: accounts + sessions + spec_versions (UNIQUE(bot_id, version), expiry indexes)                                                                                                                                                                                                                                                                                                                          |
| `apps/gateway/src/db/v12.ts` + `drizzle/0003_v12.sql`                                                                 | V1-2 tables: oauth_states (single-use consume) + interview_progress (interview-keyed, no FK) + ai_spend (live-cost meter)                                                                                                                                                                                                                                                                                           |
| `apps/gateway/src/db/v13.ts` + `drizzle/0004_v13.sql`                                                                 | V1-6 table: templates (UNIQUE(slug), category index; no reviews table until V2)                                                                                                                                                                                                                                                                                                                                     |
| `apps/gateway/src/db/seed-templates.ts` + `seed.ts`                                                                   | Idempotent template seeds (8 locked rows, ON CONFLICT DO UPDATE by slug, forks never overwritten); run `npm run seed --workspace @corvus/gateway` (`--check` smokes without DB, needs tsx)                                                                                                                                                                                                                          |
| `apps/gateway/src/db/guilds.ts` + `drizzle/0005_guilds.sql`                                                           | V1-4 table: guild_installs (UNIQUE(bot,guild), preflight scan envelope)                                                                                                                                                                                                                                                                                                                                             |
| `apps/gateway/src/db/audit-events.ts` + `drizzle/0006_audit_events.sql`                                               | V1-3 table: audit_events (append-only publish/rollback trail, no FKs, bot_id index; D-098)                                                                                                                                                                                                                                                                                                                          |
| `apps/gateway/src/start.ts` (`npm start` → `dist/start.js`)                                                           | Process boot: DATABASE_URL fail-fast + createGateway + startPreflightWorker + SIGTERM/SIGINT shutdown (D-098)                                                                                                                                                                                                                                                                                                       |
| `apps/gateway/src/lib/crypto.ts`                                                                                      | AES-256-GCM token envelope (AAD = bot id, ENCRYPTION_KEY strict 32B, CryptoError-closed)                                                                                                                                                                                                                                                                                                                            |
| `apps/gateway/src/preflight/`                                                                                         | Pure guild scanner (`scanner.ts`, Red/Yellow/Green rows, 7 checks) + pg-boss worker (`worker.ts`: decrypt → live client → scan → persist → complete; idempotent start + isWorkerRunning) + pure Red rule (`redblock.ts`: hasRed/explainRed, severity+tone tolerant; D-098)                                                                                                                                          |
| `apps/web/lib/templates/`                                                                                             | Template card/full types + slug/name validators (pure, no DB)                                                                                                                                                                                                                                                                                                                                                       |
| `apps/web/app/api/templates/`                                                                                         | Gallery list (public, card-only) + slug detail (public) + fork (auth, transactional mint + invite URL)                                                                                                                                                                                                                                                                                                              |
| `apps/web/app/api/simulate/`                                                                                          | Draft event simulator (auth, read-only, `{version, fired[]}` — prototype backend, V1-5)                                                                                                                                                                                                                                                                                                                             |
| `apps/web/lib/demo/` + `app/api/demo/` + `app/demo/`                                                                  | Scripted demo brain + rate-limited public demo route + thin prototype chat page (honest preview label, taste-deferred, V1-5)                                                                                                                                                                                                                                                                                        |
| `apps/gateway/src/store/`                                                                                             | Transactional XP store (recordXp/getXp/flush over user_records, no write-behind cache)                                                                                                                                                                                                                                                                                                                              |
| `apps/gateway/src/gateway.ts`                                                                                         | Multiplexed core: createGateway/addBot/removeBot/shutdown/quarantine, JSON-with-botId logs                                                                                                                                                                                                                                                                                                                          |
| `apps/gateway/src/launch-blockers.test.ts`                                                                            | Launch-blocker tests (real-SIGKILL durability + crash isolation + 10k storm; needs reachable PG, skips LOUDLY with a named warning when unreachable — probe + `ctx.skip`, never a hook throw, D-035)                                                                                                                                                                                                                |
| `apps/web/lib/auth/`                                                                                                  | Discord OAuth2 client + server-side sessions (getSession, PgOAuthStateStore, callback/logout orchestration) + Secure-gated cookies (name stable)                                                                                                                                                                                                                                                                    |
| `apps/web/lib/db/`                                                                                                    | Shared server pool helper (`getPool`/`__setPool`, one globalThis pool per process)                                                                                                                                                                                                                                                                                                                                  |
| `apps/web/lib/ai/`                                                                                                    | Re-export shims over `@corvus/ai` (`lanes`/`router`/`cost`/`builder-prompt` — single definition lives in `packages/ai/`) + persona streaming (`stream.ts`: reasoning/content/done events; D-105)                                                                                                                                                                                                                    |
| `apps/web/lib/invite/`                                                                                                | Capability→permission mapper + least-privilege invite URL builder (Administrator never)                                                                                                                                                                                                                                                                                                                             |
| `apps/web/lib/interview/`                                                                                             | Static question tree + pure advance/validation + session bind adapter + durable PgInterviewProgress (in-memory factory left in place for later removal — tree.test.ts still references it, D-036)                                                                                                                                                                                                                   |
| `apps/web/lib/editor/`                                                                                                | Draft read/patch helpers (pure validators, envelope checked by real parseSpec from `@corvus/spec`)                                                                                                                                                                                                                                                                                                                  |
| `packages/spec/`                                                                                                      | The single contract both apps import; drift tests fail CI on mismatch. Ships built `dist/` — run `npm run build --workspace @corvus/spec` once after install (root `ci` + CI build it first; `dist/` is gitignored). V1-5 adds pure draft-event simulation (`simulate.ts`: keyword matcher, `simulateDraft`). V1-7 adds pure explainer (`explain.ts`: spec → plain-English sentences, 20-cap, never throws; D-099). |
| `apps/web/app/demo/stats-bento/`                                                                                      | Unlinked StatsBento demo route (D-094 sandbox track; ships unguarded — guard or delete before deploy, same class as KI-013)                                                                                                                                                                                                                                                                                         |
| `infra/compose/`                                                                                                      | The only deploy artifact; `up -d` on the Hetzner box                                                                                                                                                                                                                                                                                                                                                                |
| Root tooling (`package.json`, `eslint.config.mjs`, `tsconfig.base.json`, `.prettierrc`, `.prettierignore`, `.husky/`) | Workspace wiring + gates; root `test`/`typecheck`/`format` delegate to workspaces, one lockfile at root (D-028); every workspace owns its runner + `format` script + `.prettierignore` (D-035); web eslint is ESM (`apps/web/eslint.config.mjs`, KI-001)                                                                                                                                                            |
| `infra/provision-contabo.sh`                                                                                          | One-shot box setup (Docker pinned, firewall, users, swap); verified `bash -n` clean 2026-09-07                                                                                                                                                                                                                                                                                                                      |
| `infra/RUNBOOK.md`                                                                                                    | Founder runbook: SSH in, paste, run, snapshot (§7 Deploy: pipeline + rollback)                                                                                                                                                                                                                                                                                                                                      |
| `apps/web/Dockerfile` + `apps/gateway/Dockerfile`                                                                     | Prod images (node:24-bookworm-slim; web standalone :3000, gateway dist/start.js; no secrets baked)                                                                                                                                                                                                                                                                                                                  |
| `apps/gateway/src/supervisor/`                                                                                        | V1-9 self-heal: per-bot crash counter + backoff + quarantine (opt-in via GatewayOptions.supervisor)                                                                                                                                                                                                                                                                                                                 |
| `apps/gateway/src/db/builder-runs.ts` + `drizzle/0007_builder_runs.sql`                                               | V1-7 progress: builder_runs table + `builder` pg-boss worker (brief → builder lane → draft version + pointer + ai_spend, one transaction; D-128)                                                                                                                                                                                                                                                                    |
| `packages/ai/`                                                                                                        | Shared AI layer (lanes + router + cost + builder-prompt, single definition; web + gateway import it; D-128) + budget pre-check/spans (`src/budget.ts`, D-129) + golden-brief eval harness (`src/eval/`, parseSpec parity, typechecked; D-129)                                                                                                                                                         |
| `.env.example`                                                                                                        | Secrets checklist; CI fails if code reads an unlisted var                                                                                                                                                                                                                                                                                                                                                           |

### Surface map (user-visible → code)

The bridge from founder language to files. When the founder says **"kayıt ol butonundaki şu şeyi düzelt,"** the agent resolves it through THIS table — not by reading the codebase, not by opening screenshots. One row per user-visible surface: every screen, page, flow, or control worth a name.

| Surface (name it as the founder says it)                                                     | Route / where it appears                                                                                                                                            | Main files                                                                                                                                                                                                                                                                                                                                                       | Details in                                                              |
| -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| Panel interview (setup questions + channel picker + AI diff review)                          | /interview                                                                                                                                                          | apps/web/app/interview/page.tsx (wired to start/answer/patch APIs D-124, mock fallback)                                                                                                                                                                                                                                                                          | 08 §2 Stage 1                                                           |
| Interview API (start/answer → draft spec v1)                                                 | /api/interview/*                                                                                                                                                    | apps/web/app/api/interview/start                                                                                                                                                                                                                                                                                                                                 | answer/route.ts + lib/interview/tree.ts + lib/interview/session-bind.ts | `08 §2` |
| Draft API (read / patch / publish / rollback)                                                | /api/spec/*                                                                                                                                                         | apps/web/app/api/spec/draft\|patch\|publish\|rollback/route.ts + lib/editor/drafts.ts + lib/spec/preflight.ts (409 stale-draft, Red-blocks-publish, rollback never blocked, immutable versions; D-098)                                                                                                                                                           | `08` + V1-2 SPEC                                                        |
| Pre-flight API (enqueue bot-eye guild scan / poll state)                                     | /api/preflight/start (POST) + /api/preflight?jobId= (GET poll)                                                                                                      | apps/web/app/api/preflight/start/route.ts + route.ts (auth, mapper-derived required, singleton per bot+guild) + gateway `preflight` pg-boss worker; detail Pre-flight tab runs + polls it (D-124)                                                                                                                                                                | V1-4 SPEC                                                               |
| Simulator API (fake events against a draft)                                                  | /api/simulate                                                                                                                                                       | apps/web/app/api/simulate/route.ts (auth, read-only, no Discord calls)                                                                                                                                                                                                                                                                                           | V1-5 SPEC                                                               |
| Demo window (scripted preview chat)                                                          | /demo + /api/demo/message                                                                                                                                           | Prototype page + brain + rate-limited public route (taste-deferred)                                                                                                                                                                                                                                                                                              | V1-5 SPEC                                                               |
| Bot activity feed (publish/rollback/spend)                                                   | /api/bots/[botId]/activity                                                                                                                                          | Read-only unified feed (audit + ai_spend, newest-first, ?limit 20/100; ref-gated spend, ownership-404; D-099)                                                                                                                                                                                                                                                    | 08                                                                      |
| AI chat (streaming)                                                                          | /api/chat                                                                                                                                                           | Persona-lane SSE (thinking→content→done, abort-safe, usage-unavailable→NULL spend row; D-105)                                                                                                                                                                                                                                                                    | 08                                                                      |
| Auth API (login/callback/logout)                                                             | /api/auth/*                                                                                                                                                         | apps/web/app/api/auth/*/route.ts + lib/auth/session.ts + lib/auth/discord.ts                                                                                                                                                                                                                                                                                     | `09 §1`                                                                 |
| Invite API (least-privilege URL + why-lines)                                                 | /api/invite                                                                                                                                                         | apps/web/app/api/invite/route.ts + lib/invite/permissions.ts                                                                                                                                                                                                                                                                                                     | `09 §2`                                                                 |
| Dashboard (ev: istatistik + bugün + pre-flight + şablon şeridi)                              | /dashboard                                                                                                                                                          | apps/web/app/dashboard/page.tsx + page.module.css (home-only since D-121; `?view=bots` → replace to /dashboard/bots)                                                                                                                                                                                                                                             | 08 §1                                                                   |
| Bot listesi (arama + filtre + kartlar)                                                       | /dashboard/bots                                                                                                                                                     | apps/web/app/dashboard/bots/page.tsx + page.module.css + page.test.tsx (moved verbatim from home view, D-121; New-bot → /dashboard/new)                                                                                                                                                                                                                          | 08 §1                                                                   |
| Dashboard shell + rail                                                                       | layout                                                                                                                                                              | apps/web/app/dashboard/layout.tsx + layout.module.css (Geist/skip/ambient/main + Suspense; 4 tests) + components/ui/dashboard-rail.* (SINGLE shared rail for dashboard + gallery, D-123; pathname-driven active; logout + honest-disabled Upgrade, D-124; owns its Geist font, instant first-HTML, D-125)                                                        | 08 §1                                                                   |
| Yeni bot sohbeti (oluşturma)                                                                 | /dashboard/new                                                                                                                                                      | apps/web/app/dashboard/new/page.tsx + page.module.css (standalone ChatGPT-style chat via useChatStream botId null, forceExpanded composer, back → list; 9 tests)                                                                                                                                                                                                 | 08 §2 Stage 1                                                           |
| Bot detayı (sekmeler + sohbet)                                                               | /dashboard/bots/[id]                                                                                                                                                | apps/web/app/dashboard/bots/[id]/page.tsx + page.module.css (header/actions/tabs/activity/explainer + chat via useChatStream; `?tab=` deep actions; unknown id empty state; 25 tests) + lib/bots.ts (shared mock data) + lib/chat/thread.ts + components/ui/{chat-thread,use-chat-stream,thinking-trace,prompt-input}.* (shared chat kit, Auto-only, no pickers) | 08                                                                      |
| Komponent seçici (oylama)                                                                    | /pick (unlinked)                                                                                                                                                    | apps/web/app/pick/page.tsx + variants.tsx + pick.css (fenced Tailwind no-preflight per D-073; 10 input, Turkish UI, localStorage picked-set + file-backed votes; mock, taste-gated)                                                                                                                                                                              | 04                                                                      |
| Seçim sonuçları                                                                              | /pick/results (unlinked)                                                                                                                                            | apps/web/app/pick/results/page.tsx (server, force-dynamic, reads .pick-votes/votes.json; sorted table + empty state) + app/api/pick/vote/route.ts (POST delta ±1 validated, DELETE reset; DEV-ONLY)                                                                                                                                                              | 04                                                                      |
| Landing (hero + bento + templates + pricing + FAQ)                                           | /                                                                                                                                                                   | apps/web/app/page.tsx + landing.module.css + landing-islands.tsx (D-120 Antigravity port: Vercel rhythm, non-coder copy 1:1, CTAs wired to /dashboard /demo /gallery /api/auth/login; hero asset `public/landing/dashboard-hero.jpg`)                                                                                                                            | 04                                                                      |
| Klon sayfa (birebir, geçici — SİLİNMEDİ: `/pryzm` assetleri buradan besleniyor, KI-011)      | /clone-pryzm/pryzm-clone.html (static)                                                                                                                              | apps/web/public/clone-pryzm/ (quarantined ref, DO-NOT-SHIP; delete blocked until `/pryzm` re-adapted)                                                                                                                                                                                                                                                            | 04                                                                      |
| Pryzm Next.js rotası (PARKTA — üretim `/` D-120 Antigravity portu; KI-011 re-adapt bekliyor) | /pryzm                                                                                                                                                              | apps/web/app/pryzm/page.tsx + islands.tsx + pryzm.module.css (Efferd hero + panel D-069, 41 değer D-070, tek zemin D-071, gerçek fiyat D-072, dönen H1 D-065; unlinked)                                                                                                                                                                                          | 04                                                                      |
| Açık tema denemesi (SİLİNDİ 2026-09-11, D-067)                                               | ~~`/pryzm-light`~~                                                                                                                                                  | Silindi (3 dosya)                                                                                                                                                                                                                                                                                                                                                | —                                                                       |
| Park rotalar (SİLİNDİ 2026-09-13, D-097)                                                     | ~~`/proto-nav`, `/proto-snippet`, `/proto-landing`, `/proto-pryzm`, `/pryzm-panel`, `/design-language`, `/proto-dashboard`, `/pryzm-backup`~~ + 15 dashboard yedeği | Silindi (48 dosya; yedek repo-dışı `Temp\opencode\cleanup-2026-09-13\`)                                                                                                                                                                                                                                                                                          | —                                                                       |
| Template gallery (search + filter + fork)                                                    | /gallery                                                                                                                                                            | apps/web/app/gallery/page.tsx + page.module.css (landing-card rhythm D-122; Fork → real fork API + grid ← list API with mock fallback D-124; search/filter/empty-state preserved; tests 8/8+)                                                                                                                                                                    | 08 §2 Stage 1                                                           |
| Discord login (giriş → authorize → session cookie)                                           | /api/auth/login                                                                                                                                                     | apps/web/app/api/auth/login/route.ts + lib/auth/*                                                                                                                                                                                                                                                                                                                | `09 §1`                                                                 |
| Bot listesi API (sahip olunan botlar)                                                        | /api/bots (GET)                                                                                                                                                     | apps/web/app/api/bots/route.ts (auth, own+not-deleted newest-first, 401 anon, 500 no-PG; pages live-first + mock fallback)                                                                                                                                                                                                                                       | D-126                                                                   |
| Builder ilerlemesi API (start/poll)                                                          | /api/builder/start (POST) + /api/builder?runId= (GET)                                                                                                               | apps/web/app/api/builder/_/route.ts + gateway `builder` pg-boss worker (brief → builder lane → draft version + pointer + ai_spend, one transaction) + components/ui/builder-progress._ (polls the run row; dashboard wiring open KI-014; D-128)                                                                                                                  | D-126                                                                   |
| Gizlilik / Koşullar sayfaları                                                                | /privacy, /terms                                                                                                                                                    | apps/web/app/privacy/* + apps/web/app/terms/* (static, GDPR-minimum; footer-linked from `/`)                                                                                                                                                                                                                                                                     | `09 §5`                                                                 |

Rules:

- **Add the row in the same task that creates the surface.** A surface without a row is invisible to phrase-based lookup — that is a doc bug, fix it in the same task.
- **Record the founder-language name verbatim** (Turkish) alongside the English term — the founder's phrasing is the lookup key.
- **Update the row when the surface moves or is renamed** (same task, like the file map above).
- If this table outgrows ~50 rows, promote it to a standalone `SURFACES.md` — the orchestrator adds it to the canonical list in `00_START_HERE.md` first (`Governance/HOW_TO_WRITE_DOCS.md §2`).

---

## 2. Naming conventions

> LOCKED D-018 (defaults adopted as-is):

| Thing                  | Convention            | Example                 |
| ---------------------- | --------------------- | ----------------------- |
| UI components          | PascalCase            | `PrimaryButton`         |
| Non-component modules  | kebab-case            | `storage-client`        |
| Variables, functions   | camelCase             | `getUser`               |
| Types/interfaces       | PascalCase            | `UserProfile`           |
| Module-level constants | SCREAMING_SNAKE_CASE  | `MAX_UPLOAD_MB`         |
| DB tables/columns      | snake_case            | `created_at`            |
| Test files             | co-located, `.test.*` | `PrimaryButton.test.ts` |

---

## 3. Coding standards (objective rules)

_What every piece of code in this repo must do — these are project-independent good practice._

- **Strict typing on.** No untyped escape hatches without a comment explaining why.
- **No hardcoded secrets.** Everything from environment variables. All vars listed in `.env.example` with a comment.
- **Comments explain "why," not "what."** The code says what; comments justify non-obvious decisions.
- **No premature abstraction.** Write the straightforward version first; extract a utility only at 2+ callers.
- **Respect the server/client boundary** (where the stack has one). Never leak server-only code into the client.
- **No dead code, no commented-out blocks** left behind. Delete it; history is in version control.
- **Errors are handled, not swallowed.** Fail loudly in dev, gracefully for users.

---

## 4. Linting & formatting

> LOCKED D-018: three-layer gate, exits 0 before any task is "done": `tsc --noEmit` (strict) + ESLint (flat config, no-explicit-any + no-non-null-assertion as errors in `apps/gateway`) + Prettier (printWidth 100, single quotes, trailing commas). Pre-commit hook (husky + lint-staged) runs all three on changed files. CI runs the same on the full tree.

---

## 5. Testing conventions

_Minimal but not zero during validation._

- Unit tests for pure functions (calculations, helpers, serializers).
- At least happy-path + one failure-path test for each API route/critical flow.
- Co-locate tests with the code they test.

> LOCKED D-018: **Vitest** everywhere (unit + API-route + component tests, co-located `.test.*`). Launch-blocker tests run in CI against a real `postgres:17` service container: (A) kill -9 mid-flush loses zero XP; (B) injected crash recovers silently ≤30s with no false page.

---

## 6. Commit conventions

_Conventional Commits — mandatory. Enables a readable history and clear agent PRs._

```
<type>(<scope>): <short summary>

[optional body — the "why"]
```

Types: `feat`, `fix`, `chore`, `docs`, `refactor`, `test`, `ci`.

---

## 7. Environment variables

_Every env var declared in `.env.example` with a comment explaining its purpose. Never commit a real value. CI fails if code reads an unlisted var._

Minimum set (grows with scaffolding):

```
DATABASE_URL=            # Postgres connection (gateway + web + pg-boss share it)
WIRO_API_KEY=            # wiro.ai gateway key (API Key Only project)
WIRO_BASE_URL=           # https://llm.wiro.ai/v1
OPENROUTER_API_KEY=      # fallback route (GLM-5.3-Flash)
ZAI_API_KEY=             # pinned alternate (api.z.ai)
DISCORD_CLIENT_ID=       # Corvus dashboard app (OAuth login + invites)
DISCORD_CLIENT_SECRET=   # dashboard OAuth secret (server-only, never NEXT_PUBLIC_)
CREEM_API_KEY=           # billing (test key until review passes)
CREEM_WEBHOOK_SECRET=    # webhook HMAC verification
APP_URL=                 # public base (callbacks, invites, status links)
ENCRYPTION_KEY=          # token envelope encryption (32 bytes, KMS-or-env for V1)
```
